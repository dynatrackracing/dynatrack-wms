# SNAPSHOT_FRONTEND.md — `public/index.html`

> Orientation map of the single-file frontend. Regenerate at session end if `public/index.html` changed (HAWKER_RULES rule 38).
> Generated 2026-05-28 from `public/index.html` (~1686 lines: inline CSS + inline vanilla JS, no build step, no framework). Line numbers approximate.

## Shell
- **SPA**: a fixed nav + left sidebar + `<main>` holding nine `.page` divs. `navigate(p)` (711) toggles `.active` on `#page-<p>` and the matching sidebar `.ni[data-page]`.
- **Auth**: login screen (`#login-screen`, ~131). Token stored in `localStorage` as `wms_token`; sent as `x-wms-token` by the `api()` helper (653). A 401 from `api()` triggers `showLogin()` + throws "Session expired…".
- **Top status dot** (nav) reflects **DB health only** (honest, 2026-05-28). `setSyncing()`/`setSynced()` (671/675) show transient "Syncing…/Live". **`refreshDbStatus()` (681)** drives the authoritative state from `/api/health`'s `db` field → Live / **DB Error** (only when `db≠connected`) / Offline; it's called on init + after sign-in. **`setSyncErr(err)` (695)** classifies: a 401/"session expired" → amber **"Session expired"** (`.sync-dot.warn`); any other failure → defers to `refreshDbStatus()` (so a failed eBay sync with a healthy DB stays "Live" — surfaced via toast + per-store cards, NOT the DB dot). All 9 catch sites pass the error. Toasts via `showToast(msg,isErr)` (728).

## Pages (sidebar order)
| Page | `id` | Loader fn | What it does |
|---|---|---|---|
| Dashboard | `page-dashboard` (198) | `loadDashboard` (752) | Stats cards + recent moves + **per-store eBay status cards** (one per store, rendered from `/api/ebay/health` `stores[]`, each with its own `syncStore` button); `dashSearch` (791) global lookup. |
| Scan & Move | `page-scan` (229) | `loadScanLocations` (815) | Scan input → `handleScan` (825); pick destination (`filterLocs`/`selectLoc`); `doMove` (863) calls `POST /api/move`. |
| Locations | `page-locations` (269) | `loadLocations` (906) | Zone-tabbed grid (`buildZoneTabs`/`renderLocGrid`); `addLocation` (945); `bulkAddLocations` (957). |
| Inventory | `page-inventory` (283) | `loadInventory` (976) | Status-filtered item table (`filterInvStatus`/`filterInv`); `addItem` (1007). |
| Labels | `page-labels` (302) | `loadLabels` (1026) | Barcode label generator; `autoSerial` (1066) pulls next from a sequence; `printLabel` (1080) logs to print-log. |
| eBay Orders | `page-ebay` (336) | `syncEbayOrders` (1125) | Live sync (`GET /api/ebay/orders`, all stores) or `importOrdersCSV` (1213); `renderOrders` (1260) — **Store** column/badge. |
| eBay Listings | `page-listings` (367) | `syncEbayListings` (1142) | Live sync (`GET /api/ebay/listings`, all stores) or `importListingsCSV` (1238); `renderListings` (1288) — **Store** column + per-store sub-counts. Populates in-memory `ALL_LISTINGS` (each entry **tagged with `store`**). Auto-syncs on open if empty (navigate, ~723). |
| Inventory Health | `page-health` (434) | `loadInventoryHealth` (1410) | Old-WMS-style layout (multi-store-aware). **8 stat cards:** WMS Items, eBay Inventory, Matched, eBay Only, WMS Only, Duplicate, **Cross-listed**, Staging (responsive `auto-fit` grid). Header: "Comparing N eBay listings with SKUs against M active shelf items. Staging items (S) excluded." Client-side reconcile of `ALL_LISTINGS` (union of all stores) vs WMS items; `normalizeSkuKey` (1435) strips trailing letters (rule 8); `renderHealthTable` (1528), **rows sorted by SKU asc**. eBay col = per-listing block w/ inline store badge (raw SKU · qty · green price · title[title attr] · View-on-eBay); **Cross-listed rows show BOTH stores' listings stacked** (`listingBlock`, 1570). WMS col = **plain serial (NOT linked — no item-detail page exists; never the old paid WMS)**, raw form in parens if ≠ normalized, location below; all items stacked for Duplicate; em-dash for eBay Only. Search + status chips (live counts) + store filter. Empty state = "Sync eBay listings & compare" button → calls `refreshDbStatus()` (empty listings ≠ DB error). **No CSV download icons** (architect skipped; the single header Export CSV button predates this). **Defensive:** whole function wrapped in try/catch — on error logs `[Inventory Health] render failed:` + renders a visible "failed to render — Reload Page" state (never truly blank). Top-of-function `// DIAGNOSTIC` `console.log` of input shapes (counts only) — **slated for removal** once the blank-page bug is confirmed fixed via user testing. |
| Admin | `page-admin` (393) | `loadAdmin` (1315) | Sequence management (`editSeq`/`addSeq`), bulk locations, `exportCSV` (1378) for items/locations/moves, DB + eBay status. Move-count now via `api('/moves?limit=10000')` (was a bare fetch → 401). |

## Modals
`#modal-add-location` (550), `#modal-add-item` (562), `#modal-add-seq` (576). Opened via `openModal`/`closeModal` (740–741).

## Key data facts (gotchas for future sessions)
- **eBay orders & listings are NOT persisted** — they live only in the in-memory JS arrays `ORDERS` and `ALL_LISTINGS`, repopulated per sync/import and **reset on page refresh**. Inventory Health depends on `ALL_LISTINGS` being loaded first (hence the empty-state Sync button).
- **Multi-store:** every `ORDERS`/`ALL_LISTINGS` entry carries a `store` tag (`dynatrack`/`autolumen`). `syncEbayOrders`/`syncEbayListings` hit the combined endpoints (full replace — both stores). **`syncStore(key)` (per-store) MUST replace only that store's slice via `arr.filter(x => x.store !== key).concat(fresh)` — never reassign the array, or the other store is silently wiped** (commented at the call site). `storeBadge`/`storeLabel`/`storeCountLabel` render store chips/counts.
- **SKU normalization** (`normalizeSkuKey`, 1435): `(sku||'').trim().toUpperCase().replace(/[A-Z]+$/,'')` — strips trailing letters so eBay `INT4306R` matches WMS serial `INT4306` (rule 8).
- All data access goes through `api(path,opts)` (653) → `fetch('/api'+path)` with the token header. No third-party SDKs (rule 19).
- **Top status dot = DB health only** (`refreshDbStatus`, 681). It is NOT a generic op spinner: eBay/auth/network failures must not turn it red "DB Error". A 401 → amber "Session expired"; non-DB op failures defer to `/api/health.db`. Only `db≠connected` shows "DB Error". (Fixed 2026-05-28 — `setSyncErr` used to hardcode "DB Error" for any caught failure.)

## Full function index (line → name)
600 showLogin · 605 hideLogin · 609 doLogin · 641 doLogout · 653 api · 671 setSyncing · 675 setSynced · **681 refreshDbStatus** · 695 setSyncErr · 711 navigate · 728 showToast · 740 openModal · 741 closeModal · 745 openAddLocation · 746 openAddItem · 747 openAddSeq · 752 loadDashboard · 791 dashSearch · 815 loadScanLocations · 825 handleScan · 844 filterLocs · 856 selectLoc · 863 doMove · 879 resetScan · 888 loadRecentMoves · 906 loadLocations · 917 buildZoneTabs · 925 setLocZone · 932 filterLocGrid · 934 renderLocGrid · 945 addLocation · 957 bulkAddLocations · 976 loadInventory · 999 filterInvStatus · 1005 filterInv · 1007 addItem · 1026 loadLabels · 1049 renderSeqDisplay · 1057 updateLabel · 1066 autoSerial · 1080 printLabel · 1097 storeLabel · 1098 storeBadge · 1099 storeCountLabel · 1102 mapOrder · 1115 mapListing · 1119 syncEbayNow · 1125 syncEbayOrders · 1142 syncEbayListings · 1163 syncStore · 1194 parseCSV · 1213 importOrdersCSV · 1238 importListingsCSV · 1260 renderOrders · 1285 filterOrders · 1286 filterOrderStatus · 1288 renderListings · 1309 filterListings · 1310 filterListingStock · 1315 loadAdmin · 1351 editSeq · 1363 addSeq · 1378 exportCSV · 1410 loadInventoryHealth · 1435 normalizeSkuKey · 1528 renderHealthTable · **1570 listingBlock** (nested in renderHealthTable) · 1624 filterHealth · 1625 filterHealthStore · 1627 filterHealthStatus · 1634 exportHealthCSV
