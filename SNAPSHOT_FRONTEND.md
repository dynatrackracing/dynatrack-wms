# SNAPSHOT_FRONTEND.md — `public/index.html`

> Orientation map of the single-file frontend. Regenerate at session end if `public/index.html` changed (HAWKER_RULES rule 38).
> Generated 2026-05-28 from `public/index.html` (~1591 lines: inline CSS + inline vanilla JS, no build step, no framework). Line numbers approximate.

## Shell
- **SPA**: a fixed nav + left sidebar + `<main>` holding **ten** `.page` divs (Labels removed; Pick List added 2026-05-28; **Shipped Items added 2026-05-29**). **All ten `.page` divs are direct children of `<main>` (siblings)** — verified by a depth probe. ⚠️ *2026-05-29 fix:* `#page-admin` was missing its closing `</div>`, so `#page-health` was swallowed as a child of `#page-admin` and stayed hidden (Inventory Health blank page) whenever Admin wasn't active; the missing `</div>` was restored. `navigate(p)` (693) toggles `.active` on `#page-<p>` and the matching sidebar `.ni[data-page]`. An **`@media print`** block (in `<style>`) hides `nav`/`aside`/other pages/`.no-print` so only the active page prints (used by Pick List).
- **Auth**: login screen (`#login-screen`, ~140). Token stored in `localStorage` as `wms_token`; sent as `x-wms-token` by the `api()` helper (635). A 401 from `api()` triggers `showLogin()` + throws "Session expired…".
- **Top status dot** (nav) reflects **DB health only** (honest, 2026-05-28). `setSyncing()`/`setSynced()` (653/657) show transient "Syncing…/Live". **`refreshDbStatus()` (663)** drives the authoritative state from `/api/health`'s `db` field → Live / **DB Error** (only when `db≠connected`) / Offline; it's called on init + after sign-in. **`setSyncErr(err)` (677)** classifies: a 401/"session expired" → amber **"Session expired"** (`.sync-dot.warn`); any other failure → defers to `refreshDbStatus()` (so a failed eBay sync with a healthy DB stays "Live" — surfaced via toast + per-store cards, NOT the DB dot). All catch sites pass the error. Toasts via `showToast(msg,isErr)` (710).

## Pages (sidebar order)
| Page | `id` | Loader fn | What it does |
|---|---|---|---|
| Dashboard | `page-dashboard` (206) | `loadDashboard` (734) | Stats cards + recent moves + **per-store eBay status cards** (one per store, rendered from `/api/ebay/health` `stores[]`, each with its own `syncStore` button); `dashSearch` (773) global lookup. |
| Scan & Move | `page-scan` (242) | `loadScanLocations` | Two-panel: Panel 1 scan item, Panel 2 select destination. **Robust capture (2026-05-29):** `#scan-in` uses a **gap timer** (`SCAN_FLUSH_MS`=80) — `input`→`commitScan` after keystrokes stop; Enter→immediate `commitScan` (so fast back-to-back scans don't merge/drop). `commitScan` stages exactly one serial via `handleScan` (known→stage; unknown→`#modal-intake`) and refocuses Panel 1. **Scannable destination (2026-05-29):** `#loc-filter` also gets a gap timer + Enter — `resolveLocInput` (Enter) does full resolution [exact match→`selectLoc`; single filtered→select; **zero matches→pending dest** (move auto-creates the row); ambiguous→toast], `commitLocScan` (gap) resolves **only an exact match** so manual partial-typing never auto-selects; tap-select (`filterLocs` dropdown) unchanged. `doMove` reads the stable `scannedSerial`/`selectedLocName` (never an input) → guards "Scan an item first" / "Scan or select a destination" → `POST /api/move` → success clears both inputs + refocuses Panel 1. **Working-date** control (`#working-date`/`workingDate`, sticky, resets on reload, warns when ≠ today); unknown serial → `#modal-intake` (`confirmIntake`→`POST /api/intake`). |
| Locations | `page-locations` (287) | `loadLocations` | **Name · Type · Items · View table** (rebuilt 2026-05-29 — `renderLocGrid` kept its name but now renders a table, sets `#loc-grid` `display:block` to override the `.lg` grid). Zone tabs (`buildZoneTabs`/`setLocZone`) + search (`filterLocGrid`) retained. Type badge via `locTypeBadge`/`locTypeLabel` (SHELF_BIN→"SHELF BIN", *_TOTE→"TOTE", SHIPPED). Row/View → **`openLocationDetail(name)`** → `#modal-location-detail` (fetches `GET /api/items?location=<name>`, lists Serial·Status·Notes; serials reuse `openItemHistory`; empty bin → "No items in this location"). `addLocation`/`bulkAddLocations` unchanged. |
| Inventory | `page-inventory` (290) | `loadInventory` (958) | Status-filtered item table (`filterInvStatus`/`filterInv`); `addItem` (989). **Serial cells are now clickable links** → `openItemHistory(serial)` (read-only Item History overlay) — the canonical v1 entry point. |
| eBay Orders | `page-ebay` (310) | `syncEbayOrders` (1040) | Live sync only (`GET /api/ebay/orders`, all stores); `renderOrders` (1109) — **Store** column/badge. (CSV import retired 2026-05-28.) |
| Pick List | `page-picklist` (357) | `loadPickList` (1138) | **VIEW + PRINT only** (rebuilt 2026-05-29 S4). One clean **3-column table — LOCATION · SKU · ITEM DESCRIPTION**, one row per item, from `GET /api/picklist` (flat read of `ebay_order_lines` NEEDS_PICK). Sorted location A–Z; **location-unknown items grouped at the bottom** under a "Location unknown" heading row (never dropped). No qty, no buttons, no scan field. `pickRow` helper builds each `<tr>`. **Refresh** + **Print** buttons are `.no-print`; `printPickList` (≈1180) → `window.print()`; the `@media print` block prints just this 3-col sheet. **No item mutation** — the eBay sync ships items now. (`markPicked` + `POST /api/pick` were removed.) |
| Shipped | `page-shipped` (before Admin) | `loadShipped` (1200) | **Shipped Items list** (added 2026-05-29 S5, read-only). Loads `GET /api/shipped` once into `SHIPPED_ROWS`, renders a 4-column table **SERIAL · SKU · DESCRIPTION · SHIPPED** (`renderShipped`, 1214). `SHIPPED` shows the eBay `ebay_shipped_time` (en-AU date) or **"—"** for historical baseline-imported items. Search box `filterShipped` (1231) filters `SHIPPED_ROWS` **client-side, case-insensitively across serial + sku + description**. No mutation, no eBay call. |
| eBay Listings | `page-listings` (336) | `syncEbayListings` (1057) | Live sync only (`GET /api/ebay/listings`, all stores); `renderListings` (1197) — **Store** column + per-store sub-counts. Populates in-memory `ALL_LISTINGS` (each entry **tagged with `store`**). Auto-syncs on open if empty (navigate, ~693). (CSV import retired 2026-05-28.) |
| Inventory Health | `page-health` (416) | `loadInventoryHealth` (1317) | Old-WMS-style layout (multi-store-aware). **8 stat cards:** WMS Items, eBay Inventory, Matched, eBay Only, WMS Only, Duplicate, **Cross-listed**, Staging (responsive `auto-fit` grid). Header: "Comparing N eBay listings with SKUs against M active shelf items. Staging items (S) excluded." Client-side reconcile of `ALL_LISTINGS` (union of all stores) vs WMS items. ⚠️ **Sold-out listings excluded (2026-05-29):** the reconcile first builds `liveListings = ALL_LISTINGS.filter(available>0)` (eBay keeps sold one-of-ones in ActiveList with `available`/`qty` 0); **eBay Inventory count, all buckets, and Cross-listed are computed on the live set only** (a sold-out listing is not an oversell risk). `ALL_LISTINGS` is left intact so the eBay Listings page still shows sold-out items (its own zero-stock filter). `mapListing` carries `available`. `normalizeSkuKey` (1342) strips trailing letters (rule 8); `renderHealthTable` (1435), **rows sorted by SKU asc**. eBay col = per-listing block w/ inline store badge (raw SKU · qty · green price · title[title attr] · View-on-eBay); **Cross-listed rows show BOTH stores' listings stacked** (`listingBlock`, 1477). WMS col = **plain serial (NOT linked — no item-detail page exists; never the old paid WMS)**, raw form in parens if ≠ normalized, location below; all items stacked for Duplicate; em-dash for eBay Only. Search + status chips (live counts) + store filter. Empty state = "Sync eBay listings & compare" button → calls `refreshDbStatus()` (empty listings ≠ DB error). **No CSV download icons** (architect skipped; the single header Export CSV button stays). **Defensive:** whole function wrapped in try/catch — on error logs `[Inventory Health] render failed:` + renders a visible "failed to render — Reload Page" state (never truly blank). Top-of-function `// DIAGNOSTIC` `console.log` of input shapes (counts only) — **slated for removal** once the blank-page bug is confirmed fixed via user testing. |
| Admin | `page-admin` (375) | `loadAdmin` (1224) | Sequence management (`editSeq`/`addSeq`), bulk locations, `exportCSV` (1285) for items/locations/moves, DB + eBay status. Move-count via `api('/moves?limit=10000')`. |

## Modals
`#modal-add-location`, `#modal-add-item`, `#modal-add-seq`, **`#modal-item-history`** (read-only Item History, 2026-05-29), **`#modal-intake`** (new-item intake confirm, 2026-05-29), **`#modal-location-detail`** (per-location items list, 2026-05-29; placed BEFORE `#modal-item-history` in DOM so history stacks on top when a serial is clicked from within it). Opened via `openModal`/`closeModal`; a backdrop-click handler closes any `.modal-bg`. All modals live **after `</main>`** (not `.page` sections) — no nav nesting.
- **`#modal-item-history`** is driven by **`openItemHistory(serial)`** — fetches `GET /api/items/:serial` + `GET /api/moves?serial=…&limit=1000` (read-only) and renders a header (serial · status badge · location · `intake_date` or **"unknown (legacy)"** when NULL · created_at · notes) + a chronological **oldest→newest** timeline (API is newest-first → reversed). Each event: time · `from → to` · humanized `moved_by` (`humanizeMover`: `import-baseline`→"Imported (baseline)", `ebay-sync`→"Shipped (eBay sync)", `intake`→"Added at intake", else raw). First event tagged **Intake**; any `to_location='SHIPPED'` tagged **Shipped**. Unknown serial → graceful "not found". **No mutation, no eBay call.**

## Key data facts (gotchas for future sessions)
- **eBay orders & listings are NOT persisted** — they live only in the in-memory JS arrays `ORDERS` and `ALL_LISTINGS`, repopulated per **live sync** and **reset on page refresh**. Inventory Health depends on `ALL_LISTINGS` being loaded first (hence the empty-state Sync button).
- **CSV import retired 2026-05-28** — the Dashboard "Quick Import" card and the Import-CSV buttons/file-inputs on eBay Orders & Listings were removed, along with `parseCSV`/`importOrdersCSV`/`importListingsCSV`. CSV import was fully client-side (FileReader) — no backend upload route existed. **Export CSV is retained** (Admin `exportCSV` + Inventory Health `exportHealthCSV`).
- **Multi-store:** every `ORDERS`/`ALL_LISTINGS` entry carries a `store` tag (`dynatrack`/`autolumen`). `syncEbayOrders`/`syncEbayListings` hit the combined endpoints (full replace — both stores). **`syncStore(key)` (per-store) MUST replace only that store's slice via `arr.filter(x => x.store !== key).concat(fresh)` — never reassign the array, or the other store is silently wiped** (commented at the call site). `storeBadge`/`storeLabel`/`storeCountLabel` render store chips/counts.
- **SKU normalization** (`normalizeSkuKey`, frontend 1342): `(sku||'').trim().toUpperCase().replace(/[A-Z]+$/,'')` — strips trailing letters so eBay `INT4306R` matches WMS serial `INT4306` (rule 8). ⚠️ **A byte-identical copy now also lives server-side** (`normalizeSkuKey` in server.js, used by `/api/picklist`) — keep the two in sync until centralized.
- **Pick List (rebuilt 2026-05-29 S4 — VIEW+PRINT only):** `loadPickList`/`pickRow`/`printPickList` drive `#page-picklist` off `GET /api/picklist`, which is now a **flat read of `ebay_order_lines` (NEEDS_PICK)** — no live order join, no client mutation. Renders ONE 3-column table (LOCATION · SKU · ITEM DESCRIPTION), location-unknown items grouped last. **`markPicked` and `POST /api/pick` were removed** — items are shipped automatically by the eBay orders sync (`reconcileOrderLines` Phase 2), not from this page.
- All data access goes through `api(path,opts)` (635) → `fetch('/api'+path)` with the token header. No third-party SDKs (rule 19).
- **Top status dot = DB health only** (`refreshDbStatus`, 663). It is NOT a generic op spinner: eBay/auth/network failures must not turn it red "DB Error". A 401 → amber "Session expired"; non-DB op failures defer to `/api/health.db`. Only `db≠connected` shows "DB Error". (Fixed 2026-05-28 — `setSyncErr` used to hardcode "DB Error" for any caught failure.)
- **Labels page removed 2026-05-28** (serials/barcodes are minted in a separate external system, scanned in via Scan). With it gone, two backend routes have **no frontend caller**: `POST /api/sequences/next/:prefix` and `GET`+`POST /api/print-log` (orphaned — left in server.js pending a separate decision). `GET/POST/PATCH /api/sequences` are still used by Admin's Serial Sequences section. The `.lp*` label-preview CSS classes are now dead (harmless, left in place).

## Full function index (line → name)
*(line numbers re-synced to HEAD 2026-05-29.)* 640 showLogin · 645 hideLogin · 649 doLogin · 681 doLogout · 693 api · 711 setSyncing · 715 setSynced · **721 refreshDbStatus** · 735 setSyncErr · 751 navigate · 769 showToast · 781 openModal · 782 closeModal · 786 openAddLocation · 787 openAddItem · 788 openAddSeq · **793 humanizeMover · 799 openItemHistory** · 853 loadDashboard · 892 dashSearch · 916 todayStr · 919 loadScanLocations · **927 setWorkingDate · 928 refreshWorkingDateStyle · 944 commitScan** · 960 openIntake · 968 cancelIntake · 969 confirmIntake · 991 handleScan · 1013 filterLocs · 1025 selectLoc · **1035 commitLocScan · 1042 resolveLocInput** · 1059 doMove · 1076 resetScan · 1088 loadRecentMoves · 1106 loadLocations · 1117 buildZoneTabs · 1125 setLocZone · 1132 filterLocGrid · **1135 locTypeLabel · 1141 locTypeBadge · 1147 renderLocGrid · 1169 openLocationDetail** · 1193 addLocation · 1205 bulkAddLocations · 1224 loadInventory · 1247 filterInvStatus · 1253 filterInv · 1255 addItem · 1278 storeLabel · 1279 storeBadge · 1280 storeCountLabel · 1283 mapOrder · 1296 mapListing · 1300 syncEbayNow · 1306 syncEbayOrders · 1323 syncEbayListings · 1344 syncStore · 1375 renderOrders · 1400 filterOrders · 1401 filterOrderStatus · **1407 pickRow · 1412 loadPickList · 1445 printPickList** (markPicked removed S4) · **1450 loadShipped · 1464 renderShipped · 1481 filterShipped** (added S5) · 1483 renderListings · 1504 filterListings · 1505 filterListingStock · 1510 loadAdmin · 1546 editSeq · 1557 addSeq · 1571 exportCSV · 1603 loadInventoryHealth · 1628 normalizeSkuKey + 1638 liveQty (nested in loadInventoryHealth) · 1732 renderHealthTable · 1774 listingBlock (nested) · 1828 filterHealth · 1829 filterHealthStore · 1831 filterHealthStatus · 1838 exportHealthCSV
