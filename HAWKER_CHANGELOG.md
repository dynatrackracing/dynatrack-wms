# HAWKER_CHANGELOG.md

Chronological summary of all changes to HawkerWMS. Newest entries at the TOP. Each entry: `## YYYY-MM-DD — Summary`. Append-only.

---

## 2026-05-28 — Fixed the false "DB Error" status indicator (frontend only, `public/index.html`). Root cause: the top dot was global state set by `setSyncErr()`, which hardcoded "DB Error" and was called from 9 generic catch blocks (dashboard, eBay syncs, inventory health, …) — so any eBay/auth/network failure showed as "DB Error" despite a healthy DB; never consulted `/api/health.db`. Part A: new `refreshDbStatus()` drives the dot from `/api/health.db` → Live / DB Error (only when db≠connected) / Offline (called on init + after sign-in); `setSyncErr(err)` now classifies 401→amber "Session expired" (`.sync-dot.warn`) and defers everything else to `refreshDbStatus()`; all 9 catch sites pass the error; empty Inventory Health → `refreshDbStatus()` not error. Part B: `loadAdmin`'s bare `fetch('/api/moves?limit=10000')` (no x-wms-token → 401) routed through `api()`. No auth race (createToken commits before returning; validateToken synchronous); the `/api/items` 401s were stale in-memory-session tokens (Map wiped on each deploy). Kept setSyncing/setSynced + the [Inventory Health] diagnostic log; no retries/server changes. Verified live (commit `d097c88`): /api/health 200, new strings present in served HTML — real verification needs the architect signed in. New follow-up: Postgres-backed session store (in-memory Map logs everyone out on each deploy). Regenerated SNAPSHOT_FRONTEND.md. Cutover blockers now #2 hands-on testing + #3 data extract (architect tasks).

## 2026-05-28 — Inventory Health blank-page bug: added a defensive render guard (frontend only, `public/index.html`). Diagnosis (code audit, no live repro): the `a8e2319` Health path was already fully try/caught and the markup is sound, so no data-shape throw can blank it; most likely trigger of the observed blank = a transient 401/session-expiry mid-render (`api()` shows the login overlay + throws), not an `a8e2319` logic bug. Real gap = the silent failure mode (no user-visible signal). Fix: wrapped the ENTIRE `loadInventoryHealth()` in try/catch → on error `console.error('[Inventory Health] render failed:', e)` + a visible "failed to render — Reload Page" state (never truly blank); empty-state branch moved inside the guard; added a `// DIAGNOSTIC` top-of-function `console.log` of input shapes (counts only, no PII), marked for removal. No auto-retry. Confirmed `/api/items?status=STAGED_UNLISTED` is a real endpoint (not the cause). Verified live (commit `c8f05cc`): /api/health 200, defensive markers present in served HTML — but real verification needs the architect logged in. New follow-ups: persist eBay listings server-side (`ebay_listings` table, replaces in-memory ALL_LISTINGS); remove the DIAGNOSTIC log after confirmation. Regenerated SNAPSHOT_FRONTEND.md.

## 2026-05-28 — Inventory Health UI rebuilt to the old-WMS layout (multi-store-aware), frontend only (`public/index.html`). Diagnostic: HawkerWMS has no item-detail page (only `GET /api/items/:serial`, used by the scan flow) → WMS serials render as plain text, not links (never to the old paid WMS). 8 stat cards (added WMS Items + eBay Inventory; kept Matched/eBay Only/WMS Only/Duplicate/Cross-listed/Staging) in a responsive grid. Header: "Comparing N eBay listings with SKUs against M active shelf items. Staging items (S) excluded." Table (SKU/Status/eBay/WMS) sorted by SKU asc; eBay col = per-listing block with inline store badge + raw SKU + qty + green price + truncated title (title attr) + View-on-eBay; **Cross-listed rows show BOTH stores' listings stacked** (oversell risk visible); WMS col = plain serial + raw-in-parens-if-differs + location, all items stacked for Duplicate, em-dash for eBay Only. Kept empty-state Sync button + store filter; no CSV download icons added; light theme/existing tokens. Verified live (commit `a8e2319`): /api/health 200, new UI strings present in served HTML. Regenerated SNAPSHOT_FRONTEND.md. No backend changes.

## 2026-05-28 — Multi-store eBay layer: wired AutoLumen as a second store (shared inventory; store is a property of eBay listings/orders only — no schema/inventory change). Phase 1 proposal → approval → Phase 2 build. server.js: `STORES` registry (dynatrack/autolumen, +1 entry per future store), per-store `${PREFIX}_TRADING_API_*` creds (legacy un-prefixed set IGNORED, no fallback — kept as rollback net), `validateStoreEnv()` soft-disable boot log (no throw — warehouse ops protected), store-scoped `ebayCall(store,…)`, the 3 eBay routes fan out + tag + isolate per-store errors (+`byStore`), and new `/api/ebay/:store/{health,listings,orders}`. index.html: two per-store dashboard cards + per-store `syncStore` (filter-by-store-then-concat, never array reassignment), Store columns on Orders/Listings, Inventory Health union compare + dedicated **Cross-listed** 6th bucket (SKU active on ≥2 stores = oversell risk; Duplicate unchanged) + store badges + store filter + CSV Stores column. Verified post-deploy (commit `533f83d`): both stores `connected:true`; cross-contamination gate PASS — dynatrack 3,272 vs autolumen 532 listings, disjoint ItemIDs (overlap 0). Regenerated SNAPSHOT_ROUTES.md + SNAPSHOT_FRONTEND.md. Rollback = `git revert` → single-store + un-prefixed creds. New follow-up: retire legacy un-prefixed vars once stable.

## 2026-05-28 — Ground-truth verification of #4 (no code change). A prior session died mid-flight (socket error) and a recap left doubt about whether #4 was really patched/deployed. Verified against authoritative sources: `git log` shows `741b289 Fix #4` on origin (`HEAD==origin/main==d5f028f`), current `server.js` `/api/ebay/health` uses `GetMyeBaySelling` + honest non-API-response handling, and live authenticated prod returns `{"connected":true,"message":"eBay Trading API connected"}` 3/3 — card GREEN. Conclusion: #4 was genuinely done/deployed/verified (recap was CORRECT); the died session was the AutoLumen multi-store read, which correctly touched nothing. Lesson: trust SYNC STAMP + session log + git/prod over narrative recaps. Real open item: AutoLumen multi-store wiring (diagnose-first read done, not started).

## 2026-05-28 — Fixed #4 (eBay health card "Unknown error" → GREEN). Root cause: the health probe called `GeteBayOfficialTime`, which returns an HTTP 503 `text/html` "Service Unavailable – Zero size object" gateway page (no `<Ack>`, no error envelope); `ebayCall` discards `res.statusCode` and the route collapsed it to the literal "Unknown error" (rendered verbatim by the frontend). NOT a credential issue — `GetMyeBaySelling` succeeds on the same token. Fix (`/api/ebay/health` only): probe with `GetMyeBaySelling` (1 entry, same call as listings sync, no buyer PII) so the card reflects real sync capability; a non-XML/no-`<Ack>` response now returns an honest "non-API response / likely 503" message instead of "Unknown error"; dropped the wrong "· Australia site" text (SITEID 0 = US). `ebayCall`/orders/listings/frontend untouched. Verified post-deploy via Railway-injected creds (no secrets/temp-logging): `/api/ebay/health` → `{"connected":true,"message":"eBay Trading API connected"}`, card GREEN, eBay sync healthy. Commit `741b289`; regenerated SNAPSHOT_ROUTES.md. #4 CLOSED.

## 2026-05-28 — Renamed memory files to `HAWKER_`-prefix for permanent disambiguation from the *other* eBay repo: `LAST_SESSION.md`→`HAWKER_SESSION.md`, `CHANGELOG.md`→`HAWKER_CHANGELOG.md`, `CLAUDE_RULES.md`→`HAWKER_RULES.md` (via `git mv`, history preserved). `CLAUDE.md` kept (Claude Code auto-loads it) + given a `⚠️ PROJECT IDENTITY: HawkerWMS` banner as its first lines. Updated every live internal reference to the new names (CLAUDE.md boot sequence / anti-rogue G,H,I,K,L,M / SYNC ARCHITECTURE; HAWKER_RULES Rules 3,5,6,9,36,39,40 incl. the Rule 39 signoff text; the three SNAPSHOT_* "rule 38" lines; file titles). Historical past-session entries left verbatim (old names remain only there). Prerequisite this session: reconciled a diverged `main` (rebased the 2 unpushed desktop commits onto the laptop's `2f4c513`, kept BOTH concurrent 02:39 entries) and replaced the phantom `7b9c2d8` SYNC STAMP with the real pushed hash. No app code/schema; Drive folder untouched (its stubs still carry old names → folded into follow-up #8).

## 2026-05-28 — Added CLAUDE_RULES Rule 40 (sync stamp + staleness announce) + CLAUDE.md anti-rogue item M (architect states the stamp first); amended Rule 39's signoff to include `Current stamp: <hash> @ <UTC>`. Implemented the SYNC STAMP block atop LAST_SESSION.md (updated post-push to the content commit hash). Docs only.

## 2026-05-28 — Laptop verification (follow-up #9 ✅): first clone on this laptop and first laptop→GitHub push, proving the cross-machine git round trip. Inventory (diagnostic-first): no prior clone; git 2.54.0; GCM auth (`credential.helper=manager`) cached so the clone needed no browser prompt; Auto Mode already set in `~/.claude/settings.json`; node v18.20.4/npm 10.7.0; Drive memory files already MOVED-stubs (incl. the two `(1)` conflict copies). Cloned to `C:\Users\atenr\dynatrack-wms-repo`, verified main @ `fe4fa63`, clean. Noted for future sessions: this laptop's user path is `C:\Users\atenr` — SAME as desktop, not different. No app code/schema/snapshots; nothing modified in Drive (broader cleanup remains #8).

## 2026-05-28 — Stubbed the abandoned Drive memory files (SIX: the four named + the two `(1)` duplicates `LAST_SESSION (1).md` / `CHANGELOG (1).md`) with "MOVED — DO NOT EDIT" pointers to the repo. Added CLAUDE_RULES Rule 39 (project-knowledge re-upload cadence) + CLAUDE.md anti-rogue item L. No app code/schema/snapshots. Deferred new follow-up #8 (broader Drive cleanup — stale full project copy remains) and noted #9 (laptop verification still open).

## 2026-05-28 — Inventory Health verification (no code change): confirmed the blank-page symptom was already fixed (Option B, commit `1838259`) — present in canonical code + live in prod; `/api/ebay/listings` healthy (no `ebay_listings` table — listings are never persisted); Rule 8 SKU normalization correct in code. Root cause of the repeat brief = stale claude.ai project knowledge. Added CLAUDE.md note that `Warehouse_WMS*.html` in project knowledge are old-paid-WMS snapshots (reference-only, not HawkerWMS source).

## 2026-05-28 — Folder consolidation (follow-up #1 ✅): moved the four memory files into the git repo root (canonical now `C:\Users\atenr\dynatrack-wms-repo`; Drive folder abandoned). Updated CLAUDE.md (SYNC ARCHITECTURE + anti-rogue K) and CLAUDE_RULES (Rule 31 repo-canonical; Rule 36 memory-in-repo; Rule 37 project-knowledge manual re-upload, supersedes the dropped connector idea; Rule 38 snapshot regen). Generated first SNAPSHOT_ROUTES.md / SNAPSHOT_FRONTEND.md / SNAPSHOT_SCHEMA.md. No app-code/schema changes.

## 2026-05-28 — Project-knowledge auto-sync investigated; NO supported public API to write claude.ai project knowledge (Files API is separate; only undocumented cookie endpoints exist = forbidden brittle workaround). No sync built or run. Added CLAUDE_RULES 35 (architect/worker boundary) + 36 (memory-sync reality, no fake-success). Wrote `CONNECTOR-SETUP-RUNBOOK.md` for the supported path (claude.ai Google Drive connector, one-time architect UI setup) with an empirical live-sync-vs-snapshot verification step. Backlog (the 2026-05-27 entries) still needs a one-time manual upload to project knowledge.

## 2026-05-27 — Inventory Health blank-page fix (Option B): added an actionable "Sync eBay listings & compare" button to the Health empty state (commit `1838259`, index.html Health path only). Root cause: listings live only in the in-memory `ALL_LISTINGS` array (no `ebay_listings` table) and the Health page never auto-synced — Rule 26. Verified `/api/ebay/listings` returns 3,224 real listings with the current token before patching. Reconciled master list: added "blank Inventory Health" as item #6 (closed); brief's "follow-up #1" label was a mismatch (actual #1 is folder consolidation).

## 2026-05-27 — Fixed eBay sync: diagnosed expired TRADING_API_TOKEN (via temp debug logging, reverted in 7e7fe08 over PII), Ry refreshed token in Railway → 14 GetOrders Ack=Success; located deploy in Railway project `robust-respect`, cloned canonical GitHub repo to `C:\Users\atenr\dynatrack-wms-repo` (Drive folder is not a git repo). Follow-ups: consolidate folders, hands-on testing, dashboard 503 health-card bug, token expiry ~18mo.

## 2026-05-27 — Laptop onboarding: verified synced memory files, merged Auto Mode into existing settings.json (theme preserved), created setup-claude-auto.bat; verified desktop settings.json is correctly nested.

## 2026-05-27 — Doc corrections: placeholder timestamp + rule 5 wording

Fixed the placeholder `22:00 UTC` stamp on the initial LAST_SESSION.md entry (→ `12:00 UTC`) so sessions sort correctly. Reworded CLAUDE_RULES.md rule 5 for the newest-at-top convention ("Append" → "Prepend"; clarified "append-only" = never delete old entries).

**Files touched:** LAST_SESSION.md, CLAUDE_RULES.md.

**No code or schema changes.** Documentation only.

## 2026-05-27 — Session log + changelog flipped to newest-at-top

Reversed the entry-ordering convention in `LAST_SESSION.md` and `CHANGELOG.md` from newest-at-bottom to newest-at-top to match the Darkhawk pattern. Reworded the header instruction line in both files. Existing entries reordered (one per file — cosmetic).

**Files touched:** LAST_SESSION.md, CHANGELOG.md.

**No code or schema changes.** Documentation/convention only.

## 2026-05-27 — Initial project memory system established

Set up the four-file Claude Code memory pattern (CLAUDE.md, CLAUDE_RULES.md, LAST_SESSION.md, CHANGELOG.md) mirroring the Darkhawk project. Migrated project folder from local Documents to Google Drive (`G:\My Drive\dynatrack-wms`) for cross-machine sync between desktop and laptop. Enabled Claude Code Auto Mode on desktop; created reusable setup batch file in Drive for laptop activation.

**Files touched:** CLAUDE.md, CLAUDE_RULES.md, LAST_SESSION.md, CHANGELOG.md (all in project root), `C:\Users\atenr\.claude\settings.json`, `G:\My Drive\setup-claude-auto.bat`.

**No code or schema changes.** Memory/workflow scaffolding only.
