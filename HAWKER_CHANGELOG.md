# HAWKER_CHANGELOG.md

Chronological summary of all changes to HawkerWMS. Newest entries at the TOP. Each entry: `## YYYY-MM-DD — Summary`. Append-only.

---

## 2026-05-31 — 🚀 CUTOVER: live-inventory baseline reload (HawkerWMS becomes the system of record). Clean-reloaded prod from the old-WMS final extract (wms-final-extract-2026-05-30 (6).json) as a LIVE-INVENTORY-ONLY baseline; shipped items dropped (eBay + ShippingEasy own shipped now). Architect-gated sequence; nothing destructive ran before the human Railway snapshot + explicit commit go-ahead. Decisions: import only currentLocation.locationType!=='SHIPPED' (3,390 of 5,161), all imported→STORED (remaps 6 stray STAGED_UNLISTED), intake_date=NULL for all (old WMS rewrites scan dates; age forward only). Diagnose-first caught a real bug: the script read item fields it._locationType/_locationName which DO NOT EXIST in this extract (it uses currentLocation.{locationType,name}) — would have nulled every location. Changes to scripts/import-baseline.mjs: item filter (drop SHIPPED), force status='STORED', use currentLocation fields, locations = 547 non-SHIPPED + ONE seeded empty canonical SHIPPED (skip historical SHIPPED/SHIPPED-1), intake_date left NULL, TRUNCATE ebay_order_lines in-txn (stale matched_serial; rebuilds on next sync), SHIPPED-collapse removed, garbage/null-loc kept as 0-assertions, extract path → (6) file, SAFE_MOVED_BY += ebay-sync/intake/archive/unarchive, added end-state checks; kept idempotent single-txn FK-safe clean reload. (npm install needed first — pg gitignored per-machine.) Sequence: read-only abort-guard (movers import-baseline 5061/ebay-sync 114/intake 38/dynatrack 31 — all safe) → HUMAN Railway snapshot → dry-run BEGIN…ROLLBACK reconciled to every target to the row + pre-export ~/hawker-preexport-…T18-03-32Z.json → explicit go → --commit (pre-export …T18-08-54Z.json). Post-import verify (Rule 27, fresh connection) ALL PASS: 548 locations (526 SHELF_BIN+21 UNLISTED_TOTE+1 SHIPPED), 3,390 items all STORED, 0 in SHIPPED location, 3,390 moves all import-baseline, 0 FK orphans, intake_date/archived 0, ebay_order_lines 0, sequences 12 (vestigial); /api/health 200. Expected benign side effect: first post-cutover eBay sync reconciles pre-cutover-shipped orders (dropped serials) to location_unknown → age-aware pick list routes them to the Errors tab (>3 bd), not the active sheet; they age out as eBay's window rolls forward. HUMAN follow-ups (not Claude Code): repoint tablet to hawkerwms.up.railway.app + stop using old WMS before Monday's first scan; old-WMS subscription cancel later (Ry's call). Updated SNAPSHOT_SCHEMA + HAWKER_RULES rule 27 to the new baseline (537/3,380/3,969 → 548/3,390/3,390). No schema/migration change. Rollback = Railway snapshot + two ~/hawker-preexport-*.json. Files: scripts/import-baseline.mjs, SNAPSHOT_SCHEMA.md, HAWKER_RULES.md, HAWKER_SESSION.md, HAWKER_CHANGELOG.md. Commit <pending>.

## 2026-05-31 — Multi-serial SKU tokenizer: listings packing several serials into one eBay "Custom label (SKU)" field now match EVERY component part (fixes false unlisted/unmatched). eBay READ-ONLY (Rule 25); frontend matching logic; no schema change. SCOPE = LISTED/UNLISTED only (order-reconcile/pick side PARKED). CONTEXT (now permanent in session notes): eBay SKU fields hold space-separated WMS serials + sometimes a store tag, e.g. "MOD15959V 16367V 18936V Autolumen" / "ECU0544V 0550V 0551V 0553V Autolumen" — NOT eBay Variations (0 <Variations> nodes exist); they're multi-qty listings of distinct one-of-one parts with all unit serials crammed in the SKU text. Old matcher normalized the whole string as one key → every part read unlisted. Step 0 diagnosis (read-only eBay+DB): 3,812 listings, 118 multi-serial, 96 with a store tag; whitespace delimiter; store tags AUTOLUMEN/DYNATRACK (+typo AUTOLUMENA); PREFIX-INHERITANCE confirmed make-or-break — bare tokens match only as inherited-prefix (78/91) vs as-is (9, coincidental legacy 000002-style); noise to tolerate: internal-id suffix MOD10131/000046 (take before /), trailing */commas, qty markers (3)/MOD16197(3), pure-junk words (Garage/Ford/HOLD/?). Built (public/index.html): hoisted normalizeSkuKey out of loadInventoryHealth to TOP-LEVEL (one canonical frontend copy; #14 — true centralization blocked by no-build single-file frontend Rule 18) + new listedSerialKeys(field) tokenizer (split whitespace, strip store tags/internal-id/punct/qty, prefix inheritance, normalize each → serial keys); loadInventoryHealth ebayByKey now registers each live listing under EVERY key listedSerialKeys returns (was one key per whole SKU). server.js: comment-only — normalizeSkuKey note now points at the top-level frontend location + flags listedSerialKeys must be mirrored byte-identical when the parked order-side lands (reconcileOrderLines still matches single line.sku). Verified live (read-only, against the REAL extracted functions): tokenizer resolves "ECU0139V 0144V"→[ECU0139,ECU0144], "MOD10131/000046 10221/000044"→[MOD10131,MOD10221], "MOD14075 14076 (3)"→[MOD14075,MOD14076], junk→[]; vs items (STORED active) WMS-Only/false-unlisted 1298→1009 = 289 items RESCUED; eBay-Only phantom keys 348→175; over-match check clean (352 new keys, 289 real STORED items, 63 legitimately unresolved sold/staged); node --check + inline JS OK; div balance 330/330. PARKED (not built): multi-serial pick/ship attribution — a sold unit of a multi-serial listing can't be tied to one physical serial without scan-verify (#21); that pick line should list all candidate serials. Files: public/index.html, server.js (comment), SNAPSHOT_FRONTEND.md, HAWKER_SESSION.md, HAWKER_CHANGELOG.md. Commit 9307667.

## 2026-05-30 — Capture eBay listing StartTime in ingestion (additive; eBay READ-ONLY Rule 25; no schema change). Scoped down after a read-only probe disproved the build premise. Diagnose (Step 0): fetchStoreListings (GetMyeBaySelling ActiveList, 200/page, HideVariations=false) reads only Item-level SKU/Quantity/QuantitySold/QuantityAvailable, never parses <Variations>, never captures StartTime. Probe findings: (1) ActiveList does NOT return <Variations> even with DetailLevel=ReturnAll — 0 of 3,280 dynatrack + 532 autolumen listings carried a Variations node; (2) ZERO variation listings exist in either store — the only empty-Item-SKU listings (287192249616/287356892876/397904410163) are confirmed via one-off GetItem to have hasVariations=false/varCount=0 (genuinely SKU-less flat listings, correctly unmatched); GetSellerList page 1 also 0 variations; (3) StartTime IS returned by ActiveList on 100% of listings and was being dropped. So the false-unlisted-on-variations problem isn't occurring now and the variation fix would require a forward-looking GetSellerList ingestion swap with no data to verify — architect chose "StartTime now, variations when real". Built (additive): server.js fetchStoreListings emits startTime=parseXmlValue(block,'StartTime')||null per listing (+ a NOTE that per-variation SKUs need GetSellerList and are deferred); public/index.html mapListing carries startTime onto ALL_LISTINGS (Inventory Health / cross-listed read the enriched set; no matching change since no new SKUs). Verified live (read-only): replicated the new parse vs live eBay (dynatrack page1 200 listings) → startTime present 200/200 (100%), all pass Date.parse (0 fail), sku/available unchanged; node --check + inline JS OK; div balance 330/330. DEFERRED (needs real data): per-variation SKU emission + cross-listed/oversell tightening — when a variation listing exists, source from GetSellerList (IncludeVariations=true, GranularityLevel=Fine), emit one (sku, available=Variation.Quantity−Variation.SellingStatus.QuantitySold, startTime) row per Variation; flat listings keep Item.SKU; matcher needs no further change (each variation is its own ALL_LISTINGS entry). Files: server.js, public/index.html, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md, HAWKER_SESSION.md, HAWKER_CHANGELOG.md. Commit 4be8c9d. (origin/main still at c97bd88; soft-archive + reconcile-fix + this await one authorized push.)

## 2026-05-30 — Reconcile refund/cancel-detection fix: refunded sold lines now leave NEEDS_PICK (root cause of stale pick-list strays). eBay READ-ONLY (Rule 25); no schema change. Diagnose: fetchStoreOrders parsed ship via ShippedTime (order + per-line) and cancel via OrderStatus/CheckoutStatus only; reconcileOrderLines cancelledOrder = OrderStatus∈{Cancelled,CancelPending} OR (checkout=Incomplete && paid) — never read CancelStatus or MonetaryDetails/refunds. Step 1 read-only eBay probe (via railway run --service dynatrack-wms; PII redacted) on the 3 strays: EXT869 (dynatrack, paid Apr 12) + MOD19995R (autolumen, paid Apr 7) = OrderStatus=Completed, Checkout=Complete, NO ShippedTime, RefundStatus=Succeeded (PaymentRefund) → class (b) refund missed; MOD20284 (dynatrack, paid May 29) = no ShippedTime, RefundStatus=(none) → class (c) genuinely paid+unshipped (not a bug). MonetaryDetails is present even on normal orders, so RefundStatus='Succeeded' is the distinguishing signal; no ship-detection defect found (S3's 106-item ship-move already proved ship parsing). Fix (server.js): fetchStoreOrders now emits cancelStatus=parseXmlValue(block,'CancelStatus') + refundStatus=parseXmlValue(block,'RefundStatus'); reconcileOrderLines cancelledOrder gains || cancelStatus==='CancelComplete' || refundStatus==='Succeeded'. Ship-first precedence + monotonic ON CONFLICT unchanged → shipped-then-refunded return stays SHIPPED; refunded UNshipped line → CANCELLED, its matched item correctly stays STORED (CANCELLED never triggers Phase-2 ship-move); DISMISSED never overwritten. Verified live (read-only): fixed derivation vs live eBay → EXT869 CANCELLED, MOD19995R CANCELLED, MOD20284 NEEDS_PICK (all pass); all 13 CURRENT NEEDS_PICK probed → all stay NEEDS_PICK (no over-cancel of legit picks); monotonic CASE (pure SELECT) → refunded NEEDS_PICK row flips to CANCELLED on next sync, idempotent (never downgrades); EXT869 item STORED w/ 0 matched SHIPPED lines → Phase 2 leaves it. node --check OK. CAVEATS (not fixed): (1) EXT869 + MOD19995R were DISMISSED tonight via the new Errors tab → reconcile never overwrites DISMISSED, so they stay DISMISSED + items stay STORED until hand-fixed (already off the active pick list; fix is forward-looking for future refunds); (2) anything paid >90 days ago is outside the GetOrders window and is never re-fetched. Files: server.js, SNAPSHOT_ROUTES.md, HAWKER_SESSION.md, HAWKER_CHANGELOG.md. Commit 75e30a9. (origin/main still at c97bd88; soft-archive bf52e2e+9a54e86 AND this fix are local — deploy together on next authorized push.)

## 2026-05-30 — Soft-archive (decommission/scrap): reversible removal of a live item from active inventory + every report, history retained — closes the SCRAP leak in Inventory Health (the long-pending Briefs 3a/3b). Schema change = migration (Rule 9); moves append-only (Rule 13); eBay untouched (Rule 25). Diagnose: STORED is counted STATUS-based everywhere (/api/items/count, /api/stats, /api/items [also feeds Inventory Health via ?status=STORED], reconcile match WHERE status='STORED' + Phase-2 ship, /api/locations item_count) — no location-based counting, so the FLAG (not a SCRAPPED location) is the clean mechanism (a SCRAPPED location couldn't drop items from status='STORED' counts without changing status [forbidden Rule 11] or smearing location!='SCRAPPED' everywhere). Honors Briefs 3a/3b intent (orthogonal to status, history retained, complements SHIPPED). Migration 0003 (db/migrations/0003-items-archived.sql): ADD COLUMN archived_at TIMESTAMPTZ + archive_reason TEXT (nullable, no default → existing rows stay active/NULL, not backfilled) + partial index items(archived_at) WHERE archived_at IS NOT NULL; idempotent; ACTIVE INVENTORY := archived_at IS NULL; NO new status (Rule 11). Applied to live prod via node+pg (railway run --service Postgres): columns+index present, 0 archived rows, counts unchanged (5062 items/3227 stored/544 loc/5188 moves). schema.sql NOT edited in place. Backend (server.js): gated archived_at IS NULL on /api/items (default; +?archived=1 for the archived list), /api/items/count, /api/stats counts, /api/locations item_count, reconcile match candidates + Phase-2 ship-move → Dashboard/Inventory/Inventory Health/unlisted/cross-listed/pick all auto-exclude archived. New POST /api/items/:serial/archive {reason?,moved_by='archive'} = ONE txn, guarded UPDATE archived_at=NOW()+reason WHERE archived_at IS NULL + ONE moves row→'ARCHIVED', status left as-is, location retained, 200 {archived:0} no-op. New POST /api/items/:serial/unarchive = reverse (clear archived_at/reason, guard IS NOT NULL, one moves row FROM 'ARCHIVED'→retained location or 'RESTORED'), 200 {restored:0} no-op. Frontend (public/index.html): Item History overlay shows ARCHIVED badge + archived-at/reason and a footer action — "Decommission / Scrap" (archiveItem→prompt reason→POST) on a live item or "Restore to inventory" (unarchiveItem) on an archived one (both re-open overlay + refresh Inventory/Admin if active); humanizeMover gained archive/unarchive labels; Admin gained an "Archived / Decommissioned" list (loadArchived via /items?archived=1: Serial→history · reason · last location · archived-at · [Restore], newest first, count badge). Verified live (single-txn round-trip then ROLLBACK — zero prod impact) on real STORED 000002@HR01S01: archive → storedActive 3227→3226 (−1), dropped from active items list, in archived list, NOT a pick candidate, bin count 40→39 (−1), history row retained, +1 ARCHIVED moves row; unarchive → restored to 3227; ROLLBACK → prod unchanged (3227, 0 archived rows, item moves unchanged, no stray rows). node --check server.js + inline JS OK; 11 .page divs depth-0 siblings, div balance 330/330 (Archived list lives in Admin, not a new page). STILL PENDING = Ry's authenticated browser pass (Decommission an item from its history → gone from Inventory/Dashboard/Inventory Health → Admin Archived shows it → Restore → returns to shelf+counts) + post-deploy /api/health & new routes 401. Files: db/migrations/0003-items-archived.sql, server.js, public/index.html, SNAPSHOT_SCHEMA.md, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md, HAWKER_SESSION.md, HAWKER_CHANGELOG.md. Commit bf52e2e.

## 2026-05-30 — Pick List age-aware split + retained Errors tab (stale auto-route, dismiss/restore; WMS-side writes only, no eBay Rule 25; NO schema change — read-time filter, DISMISSED already in the CHECK + protected by the reconcile ON CONFLICT). ⚠️ Laptop clone was 78 commits behind origin/main at start (HEAD 2f4c513, pre-ebay_order_lines); git pull --ff-only→0080dc2 before any work. Diagnose (Step 0): confirmed reconcileOrderLines ON CONFLICT never overwrites DISMISSED (WHEN …='DISMISSED' THEN 'DISMISSED') nor reverts SHIPPED/CANCELLED→NEEDS_PICK (dismissed stays dismissed across syncs); paid_time/first_seen already on ebay_order_lines. Backend (server.js): new HOLIDAYS Set (editable US federal holidays, seeded 2026–2027) + businessDaysSince(from,now) = US-Eastern weekdays (Mon–Fri) minus HOLIDAYS over (fromDay,today], both ends reduced to America/New_York date, day-stepped from a noon-UTC anchor (DST-safe), paid-today→0; GET /api/picklist rebuilt to add businessDaysSincePaid (paid_time, fallback first_seen)+paid_time+orderLineItemId per line and return TWO groups — active (≤3 bd, daily sort location A–Z/location-unknown last) and errors (>3 bd, most-stale first) +activeCount/errorsCount (no mutation, no line dropped); new GET /api/picklist/dismissed (retained DISMISSED archive, same shape+lastSynced, last_synced DESC, read-only); new POST /api/picklist/dismiss + /restore {orderLineItemId} = single guarded UPDATEs (NEEDS_PICK↔DISMISSED only, never SHIPPED/CANCELLED; no moves/items mutation, no eBay) → {ok,dismissed|restored}. Frontend (public/index.html): #page-picklist (loadPickList) now renders only the active group (print prints only it; sub-line shows stale count + "see Errors" + drives nav badge); new #page-picklist-errors (11th .page, direct child of <main>) + dimmed/low-prominence "Errors" sidebar entry with red count badge #nav-errors-badge (shows stale count when >0); two sections — Stale >3 bd (row + N bd + [Dismiss]) and Dismissed (row + paid date + [Restore]); new loadPickListErrors/renderPickErrorRows/pickErrRow/dismissPickLine/restorePickLine/updateErrorsNavBadge; navigate wires picklist-errors. Verified against LIVE prod DB (Node+pg via railway run — psql not installed on this laptop, and railway run -- bash can't spawn bash on Windows): real businessDaysSince → today(Fri)0, Thu1, Fri 2026-05-22→4 (Memorial Day Mon25 excluded — holiday logic proven), Apr07→37; split 13 NEEDS_PICK → 10 active (all paid today) + 3 errors (MOD18509 Mar10/58bd, MOD19995R Apr07/37bd, EXT869 Apr12/34bd) = brief's expected; dismiss/restore round-trip on the oldest stale OLI (exact route SQL) → dismiss1, leaves NEEDS_PICK + in DISMISSED, re-dismiss guard0, restore1, final NEEDS_PICK (prod left clean); guard: dismiss on a SHIPPED OLI → 0 rows, stays SHIPPED; node --check server.js + inline JS OK; 11 .page divs depth-0 siblings, div balance 319/319; /api/health 200. STILL PENDING = Ry's authenticated browser UI pass (no WMS creds on laptop): Pick List shows only ≤3-bd, Errors tab shows 3 stale + badge, Dismiss→Dismissed, Restore→back, Print = active only, re-sync doesn't resurrect a dismissed line. Behaviour note: a DISMISSED line is never auto-ship-moved even if eBay later ships it (reconcile keeps DISMISSED) — intended; restore first if it should ship. No schema change (SNAPSHOT_SCHEMA untouched). Files: server.js, public/index.html, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md, HAWKER_SESSION.md, HAWKER_CHANGELOG.md. Commit 87ac774.

## 2026-05-29 — Thread handoff / state snapshot (bookkeeping; no feature code). Shipped this thread: (a) Locations Name·Type·Items·View list + per-location detail modal (openLocationDetail/#modal-location-detail), /api/items exact `location` param (uncapped), /api/locations item_count [2944d63→e72e860→fefcf6d]; (b) Scan & Move single-flow fixes — gap-timer capture, scannable destination, guard/retention [23d8ece→490ea89→02c8b7b]; (c) Bulk Scan & Move 3-step batch + transactional POST /api/move/batch (atomic, {moved,created,location}, one moves row each), old single flow (handleScan/doMove/intake modal) REMOVED [a8096b7→597d699→6eec9bd HEAD]. All verified headless; STILL PENDING = Ry's physical tablet+Zebra acceptance test of the batch flow (the cutover gate for the daily move loop). Open notes: (i) POST /api/intake now orphaned (server.js:264; intake-modal caller removed) — flagged not deleted, like /api/sequences/next + /api/print-log; (ii) SHIPPED bin detail renders ~1,834 rows, loads fast, optional future tweak = route SHIPPED row to the Shipped page; (iii) SYNC STAMP off-by-one is by design (stamp cites the content commit; HEAD is the trailing stamp commit). NEXT UP (architect rec): persistent session store (in-memory session Map drops the tablet login on every deploy/restart — do BEFORE warehouse testing), then totes-vs-shelves dashboard split, Unlisted view, soft-archive. CLAUDE.md + HAWKER_RULES.md unchanged (no rule change). Files: HAWKER_SESSION.md, HAWKER_CHANGELOG.md.

## 2026-05-29 — Bulk Scan & Move: 3-step batch wizard (scan many → one destination, atomic commit). Replaced the two-panel single-move flow with Scan Items → Pick Location → Confirm; moves/creates ALL scanned items to one location in a single transaction. New backend POST /api/move/batch {to_location, serials:[], intake_date?}: ONE txn, all-or-nothing; ensures location row, de-dupes serials; existing serial → UPDATE STORED@to_location + one 'dynatrack' moves row (prior→to); unknown → INSERT (STORED, intake_date=given or CURRENT_DATE) + one 'intake' moves row (NULL→to); 400 on empty inputs; returns {moved,created,location}. Exactly one moves row per item (Rule 13); Item-History labels stay correct. Frontend (public/index.html) rebuilt #page-scan into 3 toggled panels (#scan-step-1/2/3, showStep): Step 1 commitScan (existing gap timer) APPENDS to batch[] via addToBatch (dedupe→toast; known→status badge; unknown→NEW badge, NO intake modal); renderBatch + removeFromBatch + live count; typed+Enter adds; Step 2 reuses filterLocs/selectLoc/commitLocScan/resolveLocInput (scan exact→select / filter+tap / unknown→pending), Back preserves list; Step 3 goToConfirm shows "Move X existing, create Y new → LOC", enumerates the Y new serials (create gate), shows workingDate, flags (was shipped); confirmBatch→POST /api/move/batch→toast+resetScan(empty Step 1)+refocus. Removed the superseded single-flow: handleScan, doMove, openIntake/confirmIntake/cancelIntake, #modal-intake. Working-date control kept. Verified (test serials only): 400 guards; batch1 create moved:0/created:2 (intake_date stamped); batch2 mix moved:2/created:1 (moved item moves = intake then dynatrack, exactly 2); ATOMIC ROLLBACK — forced bad-intake_date batch [existing,new]→500, existing item's location unchanged, new not created, no extra moves (location insert also rolled back); served HTML has step panels + new fns; node --check + inline JS OK; 10 .page divs depth-0 siblings; div balance 304/304; /api/health 200. Test cleanup: deleted 3 test items + 2 test locations (locations back to 544); synthetic moves retained (Rule 13). Ry's tablet tests (30 fast no-merge / remove mis-scan / mix→Confirm→commit / 1-item batch / typed entry / unknown doesn't interrupt) are hands-on. No schema change. Files: server.js, public/index.html, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md. Commit a8096b7.

## 2026-05-29 — Scan & Move bug fixes (frontend only, public/index.html; /api/move payload unchanged; no layout rebuild / no batch toggle [deferred #6] / no schema change; eBay untouched Rule 25). Root causes confirmed: (#2) destination wasn't scannable — a location scan landed in still-focused Panel 1, treated as a new serial, overwrote the staged item, so doMove hit !selectedLocName → "Select a destination"; (#1) single Enter capture + field-clear raced on fast back-to-back scans (merge/drop). Fix #1 robust item capture: #scan-in gap timer (SCAN_FLUSH_MS=80) — input re-arms setTimeout(commitScan,80); Enter/CR → immediate commitScan (preventDefault); commitScan reads+clears field, stages one serial via handleScan (known→stage scannedSerial; unknown→#modal-intake), refocuses Panel 1. Fix #2 scannable destination: #loc-filter gap timer + Enter — resolveLocInput (Enter) = exact match→selectLoc / single filtered→select / zero matches→selectLoc(code) pending (move auto-creates row) / ambiguous→toast; commitLocScan (gap) resolves only an exact match (manual partial-typing never auto-selects); tap-select unchanged. Fix #3 guards: doMove reads stable scannedSerial/selectedLocName (never an input) so Panel 2 typing can't clear the staged item; messages "Scan an item first" / "Scan or select a destination" (was "Select a destination"); success → resetScan, which now also clears #loc-filter + hides dropdown (both inputs) and refocuses Panel 1. Verified: served HTML has commitScan/commitLocScan/resolveLocInput/SCAN_FLUSH_MS/new input listeners/"Scan or select a destination"; node --check + inline JS OK; all 10 .page divs depth-0 siblings; div balance 304/304; /api/health 200. Functional scanner tests (3 fast serials no-merge / scan-location-into-filter→Move completes / tap-select / intake on unknown / guard messages) are Ry's tablet verification (physical HID + focus + timing). Backstop note for Ry: configure Zebra CR/Enter suffix; tap FILTER LOCATIONS before scanning the destination. No server/schema change. Files: public/index.html, SNAPSHOT_FRONTEND.md. Commit 23d8ece.

## 2026-05-29 — Locations list + per-location detail view (additive, read-only; no schema change; eBay untouched Rule 25). #page-locations was a zone-tabbed card grid with no item counts and no detail; GET /api/items only had fuzzy search+500 cap (over-matches, unusable for an exact bin). Backend: GET /api/items gained an EXACT `location` param (WHERE location=$1, ORDER BY serial ASC, UNCAPPED; precedence over fuzzy search; existing status/search/limit path intact); GET /api/locations now returns item_count (LEFT JOIN items ON location=name + GROUP BY l.id; 0 for empty bins; backward compatible). Frontend: renderLocGrid kept its name (Rule D) but now renders a Name·Type·Items·View table (#loc-grid set display:block to override the .lg grid; zone tabs + search retained; type badge via locTypeBadge/locTypeLabel SHELF_BIN→"SHELF BIN"/*_TOTE→"TOTE"/SHIPPED); row + View link → openLocationDetail(name), which fetches /api/items?location=<name> into #modal-location-detail (header name·type·count + Serial·Status·Notes; serials reuse openItemHistory; empty → "No items in this location"). Modal placed BEFORE #modal-item-history in DOM so history stacks on top. Verified live: /api/locations 544 rows w/ item_count; HR01S01 count 40 = /items?location 40 (exact, serial-ASC); SHIPPED count 1834 = 1834 (uncapped, exact); served HTML has openLocationDetail + #modal-location-detail; node --check + inline JS OK; all 10 .page divs depth-0 siblings (divs 304/304); /api/health 200. (Client serial-ASC check flagged SHIPPED false = JS localeCompare vs Postgres collation artifact, not a route bug — route applies ORDER BY serial ASC.) No schema change. Files: server.js, public/index.html, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md. Commit 2944d63.

## 2026-05-29 — New-item intake: scanning an unknown serial routes to a confirm step that explicitly creates the part (STORED, optional location, intake_date = active working date) instead of the move flow silently upserting it; plus a sticky backdatable Working-date control. Backend route + Scan & Move frontend; no schema change (intake_date from 0002); eBay untouched (Rule 25). Diagnose: handleScan 404 used to just label "NEW" then doMove→POST /api/move silently created the item; intake must intercept. New POST /api/intake {serial, location?, intake_date?, moved_by='intake'} — create-only audited txn (mirrors /api/move): existing serial → 409 {alreadyExists:true} (no clobber, caller falls back to move); else INSERT item (STORED, location or NULL, intake_date=given or CURRENT_DATE) + ensure location row if given + ONE moves row = intake event (from NULL → to shelf-or-'INTAKE' marker). Frontend Scan & Move: Working-date control (#working-date / JS workingDate; defaults today, sticky, resets to today on reload, loud warning style when ≠ today); unknown serial (404) sets scannedIsNew + opens #modal-intake (serial, optional location datalist, intake date prefilled from workingDate & editable for this item) → confirmIntake POSTs /api/intake → toast + refocus; cancel discards + refocus; doMove on a new serial routes to intake (no silent /api/move create); existing serial → unchanged move flow. humanizeMover maps intake→"Added at intake". Verified live (deployed route): empty serial→400; existing 000002→409 alreadyExists (rollback); create backdated 2026-05-15 no-location→201 STORED/location NULL/intake_date 2026-05-15 (backdate reflected); moves NULL→INTAKE by intake (1 row); appears in Inventory; re-intake same→409 (no dup); /api/health 200; served HTML has confirmIntake+#modal-intake+#working-date; node --check + inline JS OK; all 10 .page divs depth-0 siblings (divs 295/295). Test item deleted (items restored to 5061); 1 synthetic 'intake' move retained per Rule 13 (append-only; cleared at cutover). No schema change. Files: server.js, public/index.html, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md. Commit 28217a1. Deferred: Zebra/BT-HID robustness + batch modes (#6), photo-at-intake (#23), condition grading (#24).

## 2026-05-29 — Read-only Item History overlay (frontend only, public/index.html; on existing routes GET /api/items/:serial + GET /api/moves?serial=; no schema/mutation; eBay read-only Rule 25). Diagnose: items/:serial returns the full row incl. intake_date (404→api() throws→graceful); /api/moves is DESC LIMIT default 50, max moves/serial=2 so pass &limit=1000 (future-proof); moved_by in use = import-baseline (5061) + ebay-sync (106), no real-user moves yet. Added #modal-item-history (reuses the modal pattern, lives AFTER </main> — deliberately NOT a new .page section, avoiding the nesting bug just fixed). openItemHistory(serial) fetches item+moves and renders: header (serial/status badge/location/intake_date or "unknown (legacy)" when NULL/created_at/notes) + oldest→newest timeline (API DESC reversed) — each event time · from→to · humanized moved_by (humanizeMover: import-baseline→"Imported (baseline)", ebay-sync→"Shipped (eBay sync)", else raw); first event tagged Intake, any to_location='SHIPPED' tagged Shipped; unknown serial → graceful "not found". Inventory serial cells now link to openItemHistory (canonical v1 entry; reusable for Health/Shipped/Pick List later). Verified live: MOD20572 (SHIPPED, 2 moves) → Intake(Imported baseline) then Shipped(eBay sync) ordered; 000002 (STORED legacy) → single Imported(baseline), intake unknown; 42011946 (imported straight to SHIPPED) → single event [Intake][Shipped]; unknown serial → 404 graceful; served HTML has openItemHistory + clickable serial + #modal-item-history; node --check OK; all 10 .page divs still depth-0 siblings (divs 287/287); /api/health 200. (No STORED item has >1 move yet — multi-event timeline shown via a SHIPPED item; ordering is status-independent.) Files: public/index.html, SNAPSHOT_FRONTEND.md (function index re-synced to HEAD — had drifted ~27 lines). Commit 84fa366. Follow-ups (not this session): scan-to-open, wire overlay to Health/Shipped/Pick List, eBay sale enrichment via ebay_order_lines.matched_serial join.

## 2026-05-29 — Migration 0002: added `items.intake_date` DATE (nullable) — additive schema only, no app behaviour change (mirrors the S1 ebay_order_lines pattern). Foundation for the unlisted-aging view. Diagnose-first (read-only): live items columns = id/serial/status/location/notes/created_at/updated_at, no intake_date; migrations dir had only 0001; counts 544 loc/5061 items/5167 moves/14 seq. New db/migrations/0002-items-intake-date.sql: ALTER TABLE items ADD COLUMN IF NOT EXISTS intake_date DATE + CREATE INDEX IF NOT EXISTS items_intake_date_idx ON items(intake_date), idempotent in one txn; nullable, no default → baseline rows stay NULL (unknown/legacy age, not backfilled), future intake sets it. db/schema.sql NOT edited in place (Rule 9). Applied to live prod via railway --service Postgres; verified: intake_date present (date, nullable, default null), all 5061 rows NULL, index present, row counts unchanged (544/5061/5167/14), /api/health 200. Column dormant (no route reads/writes it yet). Regenerated SNAPSHOT_SCHEMA.md. Throwaway diag/apply scripts deleted. Not this session: intake-flow stamping (→ new-item intake #7), batch-date override UI, the aging view, legacy backfill.

## 2026-05-29 — Fix: exclude sold-out (available≤0) eBay listings from Inventory Health (server + frontend; eBay read-only Rule 25, ActiveList only). eBay keeps sold one-of-ones in ActiveList with QuantityAvailable 0; counting them inflated "eBay Inventory" and dumped phantom rows into "eBay Only". Diagnose: raw ActiveList confirmed QuantityAvailable present on 100% of listings and available = Quantity − SellingStatus.QuantitySold; sold-out = dynatrack 1267 + autolumen 197 = 1464 of 3812. Server fetchStoreListings now computes a robust `available` (QuantityAvailable, else Quantity − QuantitySold) and carries it on each listing (qty = same value); mapListing passes `available` through. loadInventoryHealth builds liveListings = ALL_LISTINGS.filter(available>0) and reconciles ONLY that set — eBay Inventory count, all buckets, and Cross-listed/oversell on the live set; ALL_LISTINGS kept intact so the eBay Listings page still shows sold-out items (its zero-stock filter). Verified live (replicated bucket math + post-deploy): eBay Inventory 3812→2348; eBay Only 1679→353 (1326 phantom rows removed; residual 353 are genuinely-live listings with no STORED match — real signal, not phantoms); Matched 2020→1990; WMS Only 1207→1240 (both roughly unchanged); Cross-listed 3→0 (false oversell alarms had a sold-out side); Duplicate 0. Spot-checks: sold-out INT3927V excluded; live MOD19325R→STORED MOD19325 Matched. /api/ebay/dynatrack/listings returns the `available` field; node --check + inline JS OK; /api/health 200. No DB change; independent of the un-built ebay_listings table. Files: server.js, public/index.html, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md. Commit aa74c66.

## 2026-05-29 — Fix: Inventory Health blank page — `#page-health` was mis-nested INSIDE `#page-admin` (frontend only, public/index.html). Root cause: `<div class="page" id="page-admin">` was missing its closing `</div>` (its `.g2` two-column block closed, but the outer page div did not before the Inventory Health comment), so `#page-health` was swallowed as a child of `#page-admin` and inherited its `display:none` whenever Admin wasn't the active page → Health rendered blank. Whole-file div count was 268 open / 267 close. Fix: inserted the single missing `</div>` at the admin/health boundary so `#page-admin` closes after its own content and `#page-health` becomes a direct child of `<main>` (sibling of the other pages); did NOT touch either section's inner content (+1 line). Verified on the live served HTML via a static parent-chain/depth probe across the deploy: BEFORE (old deploy) balance 268/267 with `#page-health` at depth 1 inside admin; AFTER balance 268/268 with all 10 `.page` divs at depth 0 (direct children of `<main>`), `#page-health` depth 0, final depth 0; inline JS node --check OK; /api/health 200. (The 2026-05-28 defensive render guard masked the symptom but the real cause was this DOM mis-nesting.) Files: public/index.html, SNAPSHOT_FRONTEND.md. Commit 7178b8c. No server/DB change.

## 2026-05-29 — Pick List / Shipped rework Session 5 of 5 (FINAL): new Shipped Items page (read-only; backend route + frontend page + nav; no mutation, no eBay call; Pick List + reconcile untouched). GET /api/shipped: items WHERE status='SHIPPED' LEFT JOIN ebay_order_lines (disposition='SHIPPED') on matched_serial, DISTINCT ON (serial) keeping the latest ship time; row = serial, sku (eBay sku_raw where matched else serial), description (eBay title), shippedTime (ebay_shipped_time; null for the 1724 historical baseline-imported items — NOT backfilled), store; sorted shippedTime DESC NULLS LAST → {items,count,fetched}. Frontend: new "Shipped" nav entry (after Pick List) + #page-shipped 4-column table SERIAL·SKU·DESCRIPTION·SHIPPED (loadShipped→renderShipped; "—" when no timestamp) + client-side case-insensitive search across serial/sku/description (filterShipped); navigate wires loadShipped. Verified live: /api/shipped count=1830 (106 with ebay_shipped_time+description from the S3 ship-move, 1724 historical "—"); shape {serial,sku,description,shippedTime,store}; sort non-null DESC + nulls-last confirmed; case-insensitive search confirmed ("radio"→3 desc matches, "mod"→698, lowercase "fus3267"/"ecu0245"/"mod20660" match shipped rows). Brief's "fus3205" example correctly returns none — FUS3205 is a NEEDS_PICK item (present in /api/picklist, absent from /api/shipped), not a search bug. node --check + inline JS OK; /api/health 200. No DB change; +78 LOC. Files: server.js, public/index.html, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md. Commit 1f4d118. ✅ REWORK COMPLETE (S1 table → S2 populate → S3 ship-move → S4 view+print Pick List, /api/pick removed → S5 Shipped page): the eBay orders sync now records lines, ships sold items, and feeds both the Pick List (NEEDS_PICK) and Shipped Items (SHIPPED) views. Prod 544 loc / 5061 items (3231 STORED + 1830 SHIPPED) / 5167 moves / 14 seq; warehouse still on old WMS (cutover pending).

## 2026-05-29 — Pick List / Shipped rework Session 4 of 5: Pick List rebuilt as VIEW+PRINT reading ebay_order_lines; POST /api/pick removed (backend route reshape + frontend; no item mutation here — the eBay sync ships now; eBay read-only Rule 25). Step 0: grepped every caller of /api/pick — sole caller was frontend markPicked → safe to remove. GET /api/picklist rewritten to a flat read of ebay_order_lines WHERE disposition='NEEDS_PICK' (LEFT JOIN items for CURRENT shelf): each line = location (matched item's current items.location via matched_serial; null when location_unknown), sku (matched WMS serial = the part label; sku_raw fallback only for location_unknown), description (=title), locationUnknown; sorted location A–Z, location_unknown LAST; no eBay call/days/mutation → {lines,count,fetched}. Frontend #page-picklist → ONE 3-column table LOCATION·SKU·ITEM DESCRIPTION (pickRow helper), no qty/buttons/scan, location_unknown grouped at bottom under a heading row (never dropped); Refresh+Print are .no-print. @media print cleaned to print just the 3-col sheet (removed .pick-order rule). Removed markPicked + Mark-picked button + POST /api/pick route. Verified live: /api/picklist count=6 (5 located sorted A–Z + 1 location_unknown last), line shape {location,sku,description,locationUnknown}; POST /api/pick → 404; served HTML has "Item Description", no "Mark picked"; node --check + inline JS OK; /api/health 200. (Print visual is an architect eyeball.) No DB change; net −66 LOC. Files: server.js, public/index.html, SNAPSHOT_ROUTES.md, SNAPSHOT_FRONTEND.md. Commit e9de57f. Next: S5 Shipped Items page (disposition=SHIPPED + ebay_shipped_time) completes the rework.

## 2026-05-29 — Pick List / Shipped rework Session 3 of 5: ship-move wired INTO the reconcile (the S2-deferred item mutation; backend only; triggered by the reconcile, NOT /api/pick — which was left untouched, slated for removal; no Pick List UI; eBay read-only Rule 25). STEP 1 read-only preview reported first (106 distinct STORED items would move, driven by 109 SHIPPED lines; all with real shelf locations, correct SKU→serial normalization, valid OrderLineItemIDs and ebay_shipped_time — items sold+shipped on eBay but still STORED from the 2026-05-27 import). STEP 2: added Phase 2 to `reconcileOrderLines` (after the Phase-1 upsert commits) — candidates = items still STORED with ≥1 SHIPPED matched line (one row/item); per item, ONE audited txn mirroring /api/move: SELECT…FOR UPDATE re-check status='STORED' (guard), ensure 'SHIPPED' location row, UPDATE items SET status='SHIPPED', location='SHIPPED', INSERT one moves row (from_location=prior shelf → to_location='SHIPPED', moved_by='ebay-sync'). Idempotent+monotonic (STORED guard → re-sync moves nothing; no double-move, no dup moves row); location_unknown/ambiguous never move; authoritative ship time stays on ebay_order_lines.ebay_shipped_time. Reconcile returns {upserts,skipped,moved}. Verified live: SYNC 1 moved 106, SYNC 2 moved 0 (idempotent); items STORED 3337→3231 / SHIPPED 1724→1830; moves 5061→5167 (106 ebay-sync, 106 distinct serials, 0 dup rows, all to_location='SHIPPED'); 0 items left STORED-with-SHIPPED-line; node --check OK; /api/health 200. server.js no longer "populate-only". Files: server.js, SNAPSHOT_ROUTES.md. Commit b8f5c6a. /api/pick + /api/picklist + frontend untouched. Prod now 544 loc / 5061 items (3231 STORED + 1830 SHIPPED) / 5167 moves / 14 seq. Next: S4 rebuild Pick List UI to read ebay_order_lines (NEEDS_PICK) + view/print, S5 Shipped Items page (SHIPPED + ebay_shipped_time), then remove /api/pick.

## 2026-05-29 — Pick List / Shipped rework Session 2 of 5: the eBay orders sync now POPULATES `ebay_order_lines` (populate-only; server.js only; no items/moves mutation; no /api/picklist or /api/pick change; eBay read-only Rule 25). Extended `fetchStoreOrders` additively (GetOrders DetailLevel=ReturnAll; order-level paidTime/shippedTime/checkoutStatus/paymentStatus/lastModified + per-line orderLineItemId/itemId/transactionId/lineShippedTime — picklist's consumed shape preserved). New `reconcileOrderLines` UPSERTs by order_line_item_id (fallback ItemID-TransactionID): paid = OrderStatus Completed ∧ CheckoutStatus.Status Complete ∧ eBayPaymentStatus NoPaymentFailure ∧ PaidTime; shipped = order-level ∨ per-line ShippedTime; cancelled = OrderStatus Cancelled/CancelPending ∨ refund→Incomplete; disposition shipped→SHIPPED / cancelled→CANCELLED / paid→NEEDS_PICK / else skip; match sku_norm→STORED serial (1→matched_serial; 0/>1→location_unknown, >1 never guessed; lines never dropped); monotonic ON CONFLICT (never downgrades SHIPPED/CANCELLED/DISMISSED to NEEDS_PICK; DISMISSED never overwritten; shipped/paid sticky-true; times/title/match COALESCE; last_synced=NOW()); chunked 500/txn. Hooked into /api/ebay/orders and /api/ebay/:store/orders (upsert-only → per-store sync leaves other stores intact; reconcile failure isolated in errors.reconcile, never breaks the sync). Bug found+fixed during verification: first sync had paid=false on all 1922 rows (0 NEEDS_PICK) because PaidTime/ShippedTime live AFTER <TransactionArray> and I'd parsed them from the head (correct only for CheckoutStatus) — fixed to read those from the whole block (commits f39a22b reconcile → 19402dd fix). Verified live (real sync, days=90): 1929 orders (dynatrack 1637/autolumen 292), 1928 lines upserted/1 skipped; disposition NEEDS_PICK 6 (5 matched to a STORED serial, 1 location_unknown) / SHIPPED 1849 / CANCELLED 73; paid 1850; location_unknown 1790 (shipped items no longer STORED — expected); ambiguous>1 = 0; node --check OK; /api/health 200. Items/locations/moves untouched (544/5061/5061/14). Regenerated SNAPSHOT_ROUTES.md + SNAPSHOT_SCHEMA.md (table now written, still not read by any route). Next: S3 rebuild /api/picklist + /api/pick to read/update the table (mirror /api/move audited txn; pick→real SHIPPED location, backlog #5), S4 view+print Pick List, S5 Shipped Items page.

## 2026-05-29 — Pick List / Shipped rework Session 1 of 5: added the `ebay_order_lines` backbone table (schema migration; additive; NO app-code change). Diagnose-first read of live prod (544 loc / 5061 items [3337 STORED + 1724 SHIPPED, no STAGED_UNLISTED] / 5061 moves / 14 seq; `'SHIPPED'` location id 2713 already exists; `ebay_order_lines` absent). New file `db/migrations/0001-ebay-order-lines.sql` (first in a new `db/migrations/` dir) — per Rule 9 a migration file, `db/schema.sql` NOT edited in place. Table `ebay_order_lines`, PK `order_line_item_id` (eBay OrderLineItemID = ItemID-TransactionID — never OrderID); 17 cols (store, ebay_item_id, ebay_transaction_id, sku_raw/sku_norm nullable, title nullable, paid+paid_time, shipped+ebay_shipped_time, matched_serial [nullable soft pointer, not FK], location_unknown, disposition [CHECK ∈ NEEDS_PICK/SHIPPED/CANCELLED/DISMISSED default NEEDS_PICK], first_seen, last_synced, ebay_last_modified nullable); 5 secondary indexes; idempotent (CREATE IF NOT EXISTS + INSERT 'SHIPPED' ON CONFLICT DO NOTHING — no-op, location already present). Applied to live prod via `railway run --service Postgres` and verified: all 17 cols/types/defaults correct, CHECK present, 6 indexes, row counts UNCHANGED (544/5061/5061/14), /api/health 200. Table created EMPTY and not yet wired to any route. Regenerated SNAPSHOT_SCHEMA.md (added the table + migrations note + revised the "no eBay tables" absence). Throwaway diagnostic/apply scripts deleted (no new committed scripts). No server.js/index.html/picklist/pick change. Next (sessions 2–5): sync to populate it, rebuild /api/picklist off it, view+print Pick List, Shipped Items page.

## 2026-05-29 — Documentation only (no code/schema/DB/new files): appended a "Research-Report Gap Analysis & Open Questions" subsection to the Confirmed Workflow & Build Plan and folded the enhancement candidates into its backlog (#20–#25, reconciled/cross-referenced). Recorded the unresolved OPEN QUESTION for Ry (dismantle donor vehicles vs source individual parts — gates the condition-grade/fitment work and the report's whole VIN/Hollander/core-charge model). Enhancement candidates (NOT cutover blockers, prioritized): double-sell prevention across stores [highest, extends Cross-listed], scan-to-verify at pick, returns/RMA (→ soft-archive), photo-at-intake (→ new-item intake), condition-grade/fitment (gated), ABC cycle counting + aging [lowest]. Recorded deliberate divergences (not gaps): eBay read-only; one shared login (no per-user audit attribution). No app/schema/DB changes.

## 2026-05-29 — Final import (#3) Phase 2: REAL baseline import COMMITTED to live prod (build baseline, NOT cutover). Ran `scripts/import-baseline.mjs --commit` after Ry confirmed a Railway Postgres snapshot (gate honored). FLAG 1: 59 tracking-number serials imported as-is/flagged/excluded from sequences. FLAG 2(b): added `DELETE FROM sequences` so it rebuilds to exactly the 14 computed prefixes. Run wrote a fresh commit-time pre-export (gitignored rollback artifact), abort-guard passed (all prior moves were test `dynatrack`), transactional FK-safe reload → COMMIT. Post-import verified (independent read, Rule 27): 544 locations (522 SHELF_BIN + 21 UNLISTED_TOTE + 1 SHIPPED), 5061 items (3337 STORED + 1724 SHIPPED), 5061 `import-baseline` moves, 14 sequences, 0 FK orphans, app healthy. Prod DB is now the 2026-05-27 extract baseline (realistic build/test data); warehouse still on the old WMS — final cutover extract+import still required at go-live (re-run the same script). Files: scripts/import-baseline.mjs (FLAG-2 one-liner). DB state changed; no app/schema change.

## 2026-05-29 — Final import (#3) Phase 1 (NO persistent DB write): added one-off `scripts/import-baseline.mjs` (clean-reload of the old-WMS extract; NOT app-wired; reused at cutover) + `.gitignore`. Ran read-only PRE-EXPORT (rollback artifact to ~/, gitignored) and a DRY-RUN (BEGIN…compute…ROLLBACK) against prod via the public TCP proxy (`railway run --service Postgres`; app service only has the internal DB host). Safety: dry-run default (real write needs `--commit`), abort-guard (refuses clean-reload if non-test `moved_by` exists — passed: all 3969 prod moves are test `dynatrack`), FK-safe transactional reload. Dry-run end-state matches Rule 27 prediction exactly: 544 locations (522 SHELF_BIN + 21 UNLISTED_TOTE + 1 SHIPPED), 5061 items (3337 STORED + 1724 SHIPPED), 5061 synthetic import-baseline moves, 0 FK orphans. Flags for approval: (1) 59 serials are USPS/UPS tracking numbers (shipping labels mis-scanned as serials), all in SHIPPED locations — imported+flagged+excluded from sequences; (2) sequences end at 17 (reload doesn't clear sequences; computed set includes ~5 likely-typo prefixes) — sequences vestigial. No COMMIT this session; Phase 2 (Railway snapshot → `--commit` → verify) awaits approval. Reverted an accidental package.json pg bump from npm install.

## 2026-05-29 — Documentation only (no code/schema/DB): recorded the **Confirmed Workflow & Build Plan** in HAWKER_SESSION.md. Daily workflow (clean→photo w/ SKU→shelf/tote STORED→list on eBay→sells→print pick list→ship via ShippingEasy; no ShippingEasy/eBay write-back). Device: 11" HOTWAV R9 Pro Android-14 tablet + Bluetooth Zebra HID scanner (CR suffix) — cross-cutting scan-field requirement (fire on Enter, auto-refocus, suppress soft keyboard). Locked decisions: (1) remove Staging entirely → Rule 11 = STORED/SHIPPED only; (2) "Shipped" is a LOCATION not a status → revise POST /api/pick to move into a real 'SHIPPED' location (supersedes the 2026-05-28 sentinel); (3) totes vs shelves location TYPE + dashboard split (schema, post-import); (4) Scan & Move batch + single modes; (5) new-item intake creates on unknown serial, optional location; (6) "Unlisted" = Inventory Health WMS-Only view; (7) one shared login, no offline, eBay read-only. Reconciled the open-follow-ups into one prioritized backlog (Fix Locations detail [HIGH], staging removal, pick→SHIPPED-location, Scan&Move dual+Zebra, intake, unlisted, totes, soft-archive, per-part history, persistent session store [top hardening], centralize normalizeSkuKey, dead serial-infra cleanup, etc.). Cutover still pending a final same-day extract+import; warehouse remains on the old WMS. No app/schema/DB changes.

## 2026-05-28 — Built the Print & Pick List feature (backend + frontend; no schema change; read-only to eBay per Rule 25). server.js: `fetchStoreOrders` now parses eBay `ShippedTime`→`shipped`; new `GET /api/picklist` (both stores' orders where shipped=false && status!=Cancelled, each line joined to its WMS item's shelf location via a server-side `normalizeSkuKey` byte-identical to the frontend copy — no-match lines flagged `locationUnknown` and never dropped, already-SHIPPED matches dropped, grouped per order, lines sorted by location); new `POST /api/pick {serial}` (txn mirroring /api/move: status=SHIPPED + location=NULL + ONE moves row with `to_location='SHIPPED'` sentinel — moves.to_location has no FK; verified no location named SHIPPED). public/index.html: "Pick List" nav + `#page-picklist` (card per order, Mark-picked → POST /api/pick → line drops off, Print → window.print()) + `@media print` block (single file, no libs). Verified live (commit `f54b01b`): node --check OK, inline JS 0 errors, /api/health 200, /api/picklist → 17 unshipped orders (12 location-unknown lines, surfaced not dropped), /api/pick bogus serial → 404 (no mutation). Real mark-picked happy-path + visual print pending architect (one-way inventory mutation). Regenerated SNAPSHOT_ROUTES.md + SNAPSHOT_FRONTEND.md. New follow-up: centralize the duplicated `normalizeSkuKey`.

## 2026-05-28 — Removed Import CSV from Dashboard, eBay Orders, and eBay Listings (CSV upload retired entirely). Frontend only (`public/index.html`, 83 deletions): removed the Dashboard "Quick Import" card and the `<label>Import CSV<input type=file accept=.csv>` buttons on Orders/Listings; removed the import-only JS `importOrdersCSV`/`importListingsCSV` + their shared helper `parseCSV` (no other callers); reworded leftover import-CSV copy (empty-state messages, Inventory Health summary, section comment) so no "Import CSV" strings remain. CSV import was fully client-side (FileReader) — no backend upload route exists, nothing orphaned server-side. **Export CSV untouched** (Admin items/locations/moves + Inventory Health Export CSV still work). Kept shared `ORDERS`/`ALL_LISTINGS` + render/sync. No server/db changes. Verified live (commit `de4d92f`): /api/health 200, import-CSV strings absent from served HTML, Export CSV present, inline JS compiles clean. Regenerated SNAPSHOT_FRONTEND.md.

## 2026-05-28 — Removed the Labels page (dead UI — serials/barcode labels are minted in a separate external system and scanned in via the Scan flow). Frontend only (`public/index.html`, 106 deletions): removed the Labels nav entry, `#page-labels` section, the `navigate('labels')` hook, and the 5 Labels-only functions (`loadLabels`, `renderSeqDisplay`, `updateLabel`, `autoSerial`, `printLabel`). Cleaned two cross-deps so nothing dangles: removed `loadLabels()` calls in Admin's `editSeq`/`addSeq`, and removed the init line that set `#lp-date` (lived inside page-labels; would have thrown at load). Kept shared `var seqData` (Admin Serial Sequences); left dead `.lp*` CSS. No server/db/schema changes. Reported (not changed) orphaned backend routes: `POST /api/sequences/next/:prefix` + `GET`/`POST /api/print-log` (no frontend caller now); `/api/sequences` GET/POST/PATCH still used by Admin. Verified live (commit `477de6b`): /api/health 200, Labels strings gone from served HTML, other 8 pages intact, inline JS compiles clean. Regenerated SNAPSHOT_FRONTEND.md.

## 2026-05-28 — Fixed the false "DB Error" status indicator (frontend only, `public/index.html`). Root cause: the top dot was global state set by `setSyncErr()`, which hardcoded "DB Error" and was called from 9 generic catch blocks (dashboard, eBay syncs, inventory health, …) — so any eBay/auth/network failure showed as "DB Error" despite a healthy DB; never consulted `/api/health.db`. Part A: new `refreshDbStatus()` drives the dot from `/api/health.db` → Live / DB Error (only when db≠connected) / Offline (called on init + after sign-in); `setSyncErr(err)` now classifies 401→amber "Session expired" (`.sync-dot.warn`) and defers everything else to `refreshDbStatus()`; all 9 catch sites pass the error; empty Inventory Health → `refreshDbStatus()` not error. Part B: `loadAdmin`'s bare `fetch('/api/moves?limit=10000')` (no x-wms-token → 401) routed through `api()`. No auth race (createToken commits before returning; validateToken synchronous); the `/api/items` 401s were stale in-memory-session tokens (Map wiped on each deploy). Kept setSyncing/setSynced + the [Inventory Health] diagnostic log; no retries/server changes. Verified live (commit `d097c88`): /api/health 200, new strings present in served HTML — real verification needs the architect signed in. New follow-up: Postgres-backed session store (in-memory Map logs everyone out on each deploy). Regenerated SNAPSHOT_FRONTEND.md. Cutover blockers now #2 hands-on testing + #3 data extract (architect tasks).

## 2026-05-28 — Inventory Health blank-page bug: added a defensive render guard (frontend only, `public/index.html`). Diagnosis (code audit, no live repro): the `a8e2319` Health path was already fully try/caught and the markup is sound, so no data-shape throw can blank it; most likely trigger of the observed blank = a transient 401/session-expiry mid-render (`api()` shows the login overlay + throws), not an `a8e2319` logic bug. Real gap = the silent failure mode (no user-visible signal). Fix: wrapped the ENTIRE `loadInventoryHealth()` in try/catch → on error `console.error('[Inventory Health] render failed:', e)` + a visible "failed to render — Reload Page" state (never truly blank); empty-state branch moved inside the guard; added a `// DIAGNOSTIC` top-of-function `console.log` of input shapes (counts only, no PII), marked for removal. No auto-retry. Confirmed `/api/items?status=STAGED_UNLISTED` is a real endpoint (not the cause). Verified live (commit `c8f05cc`): /api/health 200, defensive markers present in served HTML — but real verification needs the architect logged in. New follow-ups: persist eBay listings server-side (`ebay_listings` table, replaces in-memory ALL_LISTINGS); remove the DIAGNOSTIC log after confirmation. Regenerated SNAPSHOT_FRONTEND.md.

## 2026-05-28 — Inventory Health UI rebuilt to the old-WMS layout (multi-store-aware), frontend only (`public/index.html`). Diagnostic: HawkerWMS has no item-detail page (only `GET /api/items/:serial`, used by the scan flow) → WMS serials render as plain text, not links (never to the old paid WMS). 8 stat cards (added WMS Items + eBay Inventory; kept Matched/eBay Only/WMS Only/Duplicate/Cross-listed/Staging) in a responsive grid. Header: "Comparing N eBay listings with SKUs against M active shelf items. Staging items (S) excluded." Table (SKU/Status/eBay/WMS) sorted by SKU asc; eBay col = per-listing block with inline store badge + raw SKU + qty + green price + truncated title (title attr) + View-on-eBay; **Cross-listed rows show BOTH stores' listings stacked** (oversell risk visible); WMS col = plain serial + raw-in-parens-if-differs + location, all items stacked for Duplicate, em-dash for eBay Only. Kept empty-state Sync button + store filter; no CSV download icons added; light theme/existing tokens. Verified live (commit `a8e2319`): /api/health 200, new UI strings present in served HTML. Regenerated SNAPSHOT_FRONTEND.md. No backend changes.

## 2026-05-28 — Multi-store eBay layer: wired AutoLumen as a second store (shared inventory; store is a property of eBay listings/orders only — no schema/inventory change). Phase 1 proposal → approval → Phase 2 build. server.js: `STORES` registry (dynatrack/autolumen, +1 entry per future store), per-store `${PREFIX}_TRADING_API_*` creds (legacy un-prefixed set IGNORED, no fallback — kept as rollback net), `validateStoreEnv()` soft-disable boot log (no throw — warehouse ops protected), store-scoped `ebayCall(store,…)`, the 3 eBay routes fan out + tag + isolate per-store errors (+`byStore`), and new `/api/ebay/:store/{health,listings,orders}`. index.html: two per-store dashboard cards + per-store `syncStore` (filter-by-store-then-concat, never array reassignment), Store columns on Orders/Listings, Inventory Health union compare + dedicated **Cross-listed** 6th bucket (SKU active on ≥2 stores = oversell risk; Duplicate unchanged) + store badges + store filter + CSV Stores column. Verified post-deploy (commit `533f83d`): both stores `connected:true`; cross-contamination gate PASS — dynatrack 3,272 vs autolumen 532 listings, disjoint ItemIDs (overlap 0). Regenerated SNAPSHOT_ROUTES.md + SNAPSHOT_FRONTEND.md. Rollback = `git revert` → single-store + un-prefixed creds. New follow-up: retire legacy un-prefixed vars once stable.

## 2026-05-28 — Ground-truth verification of #4 (no code change). A prior session died mid-flight (socket error) and a recap left doubt about whether #4 was really patched/deployed. Verified against authoritative sources: `git log` shows `741b289 Fix #4` on origin (`HEAD==origin/main==d5f028f`), current `server.js` `/api/ebay/health` uses `GetMyeBaySelling` + honest non-API-response handling, and live authenticated prod returns `{"connected":true,"message":"eBay Trading API connected"}` 3/3 — card GREEN. Conclusion: #4 was genuinely done/deployed/verified (recap was CORRECT); the died session was the AutoLumen multi-store read, which correctly touched nothing. Lesson: trust SYNC STAMP + session log + git/prod over narrative recaps. Real open item: AutoLumen multi-store wiring (diagnose-first read done, not started).

## 2026-05-28 — Fixed #4 (eBay health card "Unknown error" → GREEN). Root cause: the health probe called `GeteBayOfficialTime`, which returns an HTTP 503 `text/html` "Service Unavailable – Zero size object" gateway page (no `<Ack>`, no error envelope); `ebayCall` discards `res.statusCode` and the route collapsed it to the literal "Unknown error" (rendered verbatim by the frontend). NOT a credential issue — `GetMyeBaySelling` succeeds on the same token. Fix (`/api/ebay/health` only): probe with `GetMyeBaySelling` (1 entry, same call as listings sync, no buyer PII) so the card reflects real sync capability; a non-XML/no-`<Ack>` response now returns an honest "non-API response / likely 503" message instead of "Unknown error"; dropped the wrong "· Australia site" text (SITEID 0 = US). `ebayCall`/orders/listings/frontend untouched. Verified post-deploy via Railway-injected creds (no secrets/temp-logging): `/api/ebay/health` → `{"connected":true,"message":"eBay Trading API connected"}`, card GREEN, eBay sync healthy. Commit `741b289`; regenerated SNAPSHOT_ROUTES.md. #4 CLOSED.

## 2026-05-28 — Renamed memory files to `HAWKER_`-prefix for permanent disambiguation from the *other* eBay repo: `LAST_SESSION.md`→`HAWKER_SESSION.md`, `CHANGELOG.md`→`HAWKER_CHANGELOG.md`, `CLAUDE_RULES.md`→`HAWKER_RULES.md` (via `git mv`, history preserved). `CLAUDE.md` kept (Claude Code auto-loads it) + given a `⚠️ PROJECT IDENTITY: HawkerWMS` banner as its first lines. Updated every live internal reference to the new names (CLAUDE.md boot sequence / anti-rogue G,H,I,K,L,M / SYNC ARCHITECTURE; HAWKER_RULES Rules 3,5,6,9,36,39,40 incl. the Rule 39 signoff text; the three SNAPSHOT_* "rule 38" lines; file titles). Historical past-session entries left verbatim (old names remain only there). Prerequisite this session: reconciled a diverged `main` (rebased the 2 unpushed desktop commits onto the laptop's `2f4c513`, kept BOTH concurrent 02:39 entries) and replaced the phantom `7b9c2d8` SYNC STAMP with the real pushed hash. No app code/schema; Drive folder untouched (its stubs still carry old names → folded into follow-up #8).

## 2026-05-28 — Added CLAUDE_RULES Rule 40 (sync stamp + staleness announce) + CLAUDE.md anti-rogue item M (architect states the stamp first); amended Rule 39's signoff to include `Current stamp: <hash> @ <UTC>`. Implemented the SYNC STAMP block atop LAST_SESSION.md (updated post-push to the content commit hash). Docs only.

## 2026-05-28 — Laptop verification (follow-up #9 ✅): first clone on this laptop and first laptop→GitHub push, proving the cross-machine git round trip. Inventory (diagnostic-first): no prior clone; git 2.54.0; GCM auth (`credential.helper=manager`) cached so the clone needed no browser prompt; Auto Mode already set in `~/.claude/settings.json`; node v18.20.4/npm 10.7.0; Drive memory files already MOVED-stubs (incl. the two `(1)` conflict copies). Cloned to `C:\Users\atenr\dynatrack-wms-repo`, verified main @ `fe4fa63`, clean. Noted for future sessions: this laptop's user path is `C:\Users\atenr` — SAME as desktop, not different. No app code/schema/snapshots; nothing modified in Drive (broader cleanup remains #8).

## 2026-05-28 — Stubbed the abandoned Drive memory files (SIX: the four named + the two `(1)` duplicates `LAST_SESSION (1).md` / `CHANGELOG (1).md`) with "MOVED — DO NOT EDIT" pointers to the repo. Added CLAUDE_RULES Rule 39 (project-knowledge re-upload cadence) + CLAUDE.md anti-rogue item L. No app code/schema/snapshots. Deferred new follow-up #8 (broader Drive cleanup — stale full project copy remains) and noted #9 (laptop verification still open).

## 2026-05-28 — Inventory Health verification (no code change): confirmed the blank-page symptom was already fixed (Option B, commit `1838259`) — present in canonical code + live in prod; `/api/ebay/listings` healthy (no `ebay_listings` table — listings are never persisted); Rule 8 SKU normalization correct in code. Root cause of the repeat brief = stale claude.ai project knowledge. Added CLAUDE.md note that `Warehouse_WMS*.html` in project knowledge are old-paid-WMS snapshots (reference-only, not HawkerWMS source).

## 2026-05-28 — Folder consolidation (follow-up #1 ✅): moved the four memory files into the git repo root (canonical now `C:\Users\atenr\dynatrack-wms-repo`; Drive folder abandoned). Updated CLAUDE.md (SYNC ARCHITECTURE + anti-rogue K) and CLAUDE_RULES (Rule 31 repo-canonical; Rule 36 memory-in-repo; Rule 37 project-knowledge manual re-upload, supersedes the dropped connector idea; Rule 38 snapshot regen). Generated first SNAPSHOT_ROUTES.md / SNAPSHOT_FRONTEND.md / SNAPSHOT_SCHEMA.md. No app-code/schema changes.

## 2026-05-28 — Project-knowledge auto-sync investigated; NO supported public API to write claude.ai project knowledge (Files API is separate; only undocumented cookie endpoints exist = forbidden brittle workaround). No sync built or run. Added CLAUDE_RULES 35 (architect/worker boundary) + 36 (memory-sync reality, no fake-success). Wrote `CONNECTOR-SETUP-RUNBOOK.md` for the supported path (claude.ai Google Drive connector, one-time architect UI setup) with an empirical live-sync-vs-snapshot verification step. Backlog (the 2026-05-27 entries) still needs a one-time manual upload to project knowledge.

## 2026-05-27 — Inventory Health blank-page fix (Option B): added an actionable "Sync eBay listings & compare" button to the Health empty state (commit `1838259`, index.html Health path only). Root cause: listings live only in the in-memory `ALL_LISTINGS` array (no `ebay_listings` table) and the Health page never auto-synced — Rule 26. Verified `/api/ebay/listings` returns 3,224 real listings with the current token before patching. Reconciled master list: added "blank Inventory Health" as item #6 (closed); brief's "follow-up #1" label was a mismatch (actual #1 is folder consolidation).

## 2026-05-27 — Fixed eBay sync: diagnosed expired TRADING_API_TOKEN (via temp debug logging, reverted in 7e7fe08 over PII), Ry refreshed token in Railway → 14 GetOrders Ack=Success; located deploy in Railway project `robust-respect`, cloned canonical GitHub repo to `C:\Users\atenr\dynatrack-wms-repo` (Drive folder is not a git repo). Follow-ups: consolidate folders, hands-on testing, dashboard 503 health-card bug, token expiry ~18mo.

## 2026-05-27 — Laptop onboarding: verified synced memory files, merged Auto Mode into existing settings.json (theme preserved), created setup-claude-auto.bat; verified desktop settings.json is correctly nested.

## 2026-05-27 — Doc corrections: placeholder timestamp + rule 5 wording

Fixed the placeholder `22:00 UTC` stamp on the initial LAST_SESSION.md entry (→ `12:00 UTC`) so sessions sort correctly. Reworded CLAUDE_RULES.md rule 5 for the newest-at-top convention ("Append" → "Prepend"; clarified "append-only" = never delete old entries).

**Files touched:** LAST_SESSION.md, CLAUDE_RULES.md.

**No code or schema changes.** Documentation only.

## 2026-05-27 — Session log + changelog flipped to newest-at-top

Reversed the entry-ordering convention in `LAST_SESSION.md` and `CHANGELOG.md` from newest-at-bottom to newest-at-top to match the Darkhawk pattern. Reworded the header instruction line in both files. Existing entries reordered (one per file — cosmetic).

**Files touched:** LAST_SESSION.md, CHANGELOG.md.

**No code or schema changes.** Documentation/convention only.

## 2026-05-27 — Initial project memory system established

Set up the four-file Claude Code memory pattern (CLAUDE.md, CLAUDE_RULES.md, LAST_SESSION.md, CHANGELOG.md) mirroring the Darkhawk project. Migrated project folder from local Documents to Google Drive (`G:\My Drive\dynatrack-wms`) for cross-machine sync between desktop and laptop. Enabled Claude Code Auto Mode on desktop; created reusable setup batch file in Drive for laptop activation.

**Files touched:** CLAUDE.md, CLAUDE_RULES.md, LAST_SESSION.md, CHANGELOG.md (all in project root), `C:\Users\atenr\.claude\settings.json`, `G:\My Drive\setup-claude-auto.bat`.

**No code or schema changes.** Memory/workflow scaffolding only.